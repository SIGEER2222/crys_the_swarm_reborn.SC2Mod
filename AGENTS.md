# AGENTS

本仓库默认采用高自治执行模式。后续进入此仓库的 Codex / coding agent，应优先按下面规则工作，而不是频繁停下来等待人工确认。

## 1. 默认目标

- 目标是闭环交付，不是只做分析。
- 默认允许自主排查、自主实现、自主验证、自主补文档。
- 除非遇到明确高风险阻塞，否则不要中途停下来等待用户确认。

## 2. 没有专用工具时的默认行为

即使没有现成脚本、自动化工具或专门 helper，也应继续推进，而不是因为“缺工具”直接停下。

默认做法：

1. 先读代码、文档、脚本和最近改动。
2. 基于仓库现状做保守假设。
3. 先用仓内已有能力完成 80% 工作。
4. 工具缺口只在真的影响效率或可复用性时再补。
5. 如果不补工具也能完成目标，优先直接完成目标。

## 3. 长任务的工作方式

对于预计超过 30 分钟的任务，默认采用“连续推进”模式：

1. 自行拆分阶段。
2. 每个阶段完成后直接进入下一阶段。
3. 每轮都做最小必要验证。
4. 同步更新相关文档，避免下一轮从零理解。
5. 不因为“做完一半”而停下等用户确认。

如果任务跨多个子系统，优先顺序应为：

1. 依赖链
2. 运行时入口
3. 核心逻辑
4. 地图或场景分支
5. 文档和验证

## 4. 遇到不确定性时的默认策略

### 可以自行决定的情况

- 文件放在哪个已有目录更合理
- 先改哪一层更稳
- 是否补静态校验脚本
- 是否顺手整理重复文档
- 是否把阶段记录归档
- 是否补一份当前状态说明

### 应先自救再决定的情况

- 代码看起来冲突：先读相关文件和 `git diff`
- 逻辑不清楚：先全文搜索调用链
- 文档有多份说法：先以当前代码为准重写
- 缺验证入口：先找现有脚本、日志、Bank、地图入口

### 只有这些情况才允许打断用户

1. 会做破坏性操作
2. 会覆盖无法判断归属的现有改动
3. 必须人工登录、人工点击、验证码、外部权限
4. 连续多轮排查后仍没有客观可推进路径

## 5. 交付标准

默认不要把以下状态当成“已完成”：

- 只有字符串命中
- 只有入口加了名字
- 只有文档或计划
- 只有一半链路接上
- 只有静态存在，没有实际验证

默认完成标准应尽量接近：

1. 代码已落地
2. 相关链路已对齐
3. 跑过最小必要验证
4. 文档已更新到当前状态
5. 明确写出剩余风险，而不是把未完成内容藏起来

## 6. 本仓库的特殊约束

### SC2 / 指挥官项目

- `XMFinal.SC2Mod` 优先作为统一 runtime owner。
- 新指挥官或新机制优先接到统一框架，不要把通用逻辑散到每张地图。
- SC2 新增/活跃维护的 runtime 代码文件默认不超过 1000 行；特别是 `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/*.galaxy` 与 `原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/*.galaxy`，新增逻辑应按职责拆成 include/profile/helper 文件。可用 `scripts/sc2/check-galaxy-line-limit.ps1` 校验。历史地图脚本、`t3Terrain.xml`、官方/原始导出 XML 这类生成或遗留资源不要为满足行数而盲拆。
- `crys_the_swarm_reborn.SC2Mod` 只可作参考，不可直接当来源。
- Launcher 能显示，不代表玩法已完成。
- 当前有两条本地工程线：`合作指挥官版起义狂潮/` 重新作为当前开发 owner，用于继续完成旧线开发；`原始mod/` 是拿原 mod 续上的新项目，用于测试新写法和回归。
- `合作指挥官版起义狂潮/` 可以作为当前实现状态与开发目标，但不要当作官方数据或最终设计依据；其中链路仍需逐项验证，不能因为 Launcher 能显示就视为玩法已完成。
- 官方原始合作指挥官数据以 `references/sc2-build-96883-casc-export` 为准；该目录来自 SC2 Build 96883 CASC 导出。
- 指挥官等级、精通、威望设计优先读取 `游戏数据/官方合作指挥官/commanders/<Commander>/`，再用 `references/sc2-build-96883-casc-export/` 追完整 Catalog / UserData / 触发器闭包；`合作指挥官版起义狂潮/` 是当前实现目标，`原始mod/` 是新写法和回归线，二者都不能反推官方设计基准。
- 提取官方等级加点/精通时，读取 `mods/starcoop/starcoop.sc2mod/base.sc2data/gamedata/userdata.xml` 里的 `CampaignPerk` / `MasteryUpgrades`，并补读 `mods/starcoop/commanders/egonstetmann.sc2mod` 与 `mods/starcoop/commanders/arcturusmengsk.sc2mod` 的 `userdata.xml`。
- 不要把 `合作指挥官版起义狂潮/Mods/XM/**/CommanderAch` 当作官方原始数据源；它是当前 Mod 的 runtime/custom 映射。
- 当前官方合作指挥官加点/精通提取脚本：`scripts/sc2/export-official-coop-progression.py`；当前输出目录：`docs/每日进度/2026-05-26官方合作指挥官原始精通加点/`。
- 雷诺强度融合当前以 `docs/指挥官威望/雷诺精通威望加点融合设计-2026-05-27.md` 为准：等级 15 全解锁、6 项精通全满；威望只取选定收益，不直接启用官方三个 `PlayerPrestige`。

### 文档

- 多份阶段文档冲突时，以当前代码和最新验证为准。
- 过期阶段稿优先归档，不要继续让它们占据主入口。
- 当前有效文档应保持少量、清晰、可直接接手。
- `docs/MPQEditor控制台指令速查.md` 是 MPQ 打包/解包时的控制台命令速查，优先用于查 `open`、`add`、`extract`、`compact` 这类命令和脚本写法。
- 查这份文档时先看“进入方式”和“命令表”，再看“常用例子”与“注意点”；需要实操时优先用仓内现成脚本或脚本文件驱动 `/console`。

## 7. 长跑任务的默认输出

如果任务持续时间较长，最终输出应包含：

1. 做了什么
2. 现在哪些已经闭环
3. 哪些仍是剩余风险
4. 下次继续时应该从哪里接

如果中途需要保留上下文，优先在仓库内留下：

- 当前状态文档
- 归档说明
- 验证脚本
- 结果日志

而不是把关键判断只留在会话里。
