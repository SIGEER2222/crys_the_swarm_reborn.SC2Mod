# 原始mod 当前状态

## 当前结论

`原始mod` 这条线已经不是空白试验目录，而是具备完整 `XMCore + XMFinal + CommanderTestBench + Launcher` 的新开发基线。

当前可以确认：

- `原始mod/Maps/XM/Launcher.SC2Map` 存在。
- `原始mod/Maps/XM/CommanderTestBench.SC2Map` 存在。
- `XMFinal.SC2Mod/Base.SC2Data` 已拆分为多份 galaxy 文件，包含：
  - `LibE0EAE146_TestBench.galaxy`
  - `LibE0EAE146_TestBenchCore.galaxy`
  - `LibE0EAE146_CommanderRosters.galaxy`
  - `LibE0EAE146_CommanderBuildings.galaxy`
  - `LibE0EAE146_CommanderPanels.galaxy`
  - `LibE0EAE146_CommanderTech.galaxy`
  - `LibE0EAE146_CommanderHeroAbilities.galaxy`
  - `LibE0EAE146_CommanderUnitAbilities.galaxy`

## 当前测试能力

`CommanderTestBench` 调度层已经支持以下场景入口：

- `full_units`
- `full_buildings`
- `level15_units`
- `fusion_final_units`
- `panel_cost_smoke`
- `panel_effect_smoke`
- `hero_ability_smoke`
- `unit_ability_smoke`
- `unit_evolution_smoke`
- `tech_smoke`

其中与本阶段最相关的是：

- `full_units`：按指挥官兵种清单直接生成单位。
- `full_buildings`：按指挥官建筑清单直接生成建筑。

## 本阶段补充

本轮已补：

- 新建本地状态文档，避免 `原始mod` 继续没有 handoff 入口。
- 已落地 `scripts/sc2/export-originalmod-testbench-rosters.ps1`，可从 `CommanderRosters` / `CommanderBuildings` 自动导出“兵种 + 建筑”中文清单。
- 已生成 `docs/每日进度/2026-05-28-原始mod-CommanderTestBench兵种与建筑清单.md`，可直接作为 `full_units + full_buildings` 的测试对照表。
- 中文名导出已接入两级来源：
  - 优先读取 `原始mod/Mods/XM/*/zhCN.SC2Data/LocalizedData/GameStrings.txt`
  - 缺失时回退到 `references/sc2-build-96883-casc-export` 官方 `zhcn` GameStrings
  - 对官方/现有本地化都缺失的少量条目，已在导出脚本中补最终显示映射
- 已完成 live 部署验证：
  - `scripts/deploy-originalmod-testbench.ps1` 已成功把 `原始mod` 的 `Mods/XM/*` 与 `CommanderTestBench.SC2Map` 同步到 `E:\SC2\SC2new\StarCraft II`
  - live 目标路径 `E:\SC2\SC2new\StarCraft II\Maps\XM\CommanderTestBench.SC2Map` 已存在
- 已完成启动诊断与口径修正：
  - 新增 `scripts/diagnose-originalmod-launch.ps1`
  - 早期诊断报告见 `tmp/launch-diagnostics/launch-diagnostics-20260528-211535.md`
  - 该报告里的“`SC2Switcher_x64.exe` 直进 `CommanderTestBench.SC2Map` 会立即退出且不会拉起 `SC2_x64`”已被后续实机验证推翻
  - 当前这台机器上，使用
    `& "E:\SC2\SC2new\StarCraft II\Support64\SC2Switcher_x64.exe" "E:\SC2\SC2new\StarCraft II\Maps\XM\CommanderTestBench.SC2Map"`
    可以拉起 `SC2_x64`
  - 现阶段真实阻塞已经收缩到“自动化脚本向游戏注入 chat 命令不稳定”，不是地图直进命令本身无效

## 当前不足

- `原始mod` 之前没有 `docs/`，接手成本高。
- 现阶段确认的是“测试框架与清单已接上”，不是“所有指挥官都已实测通过”。
- 当前清单的兵种/建筑名已基本收口；剩余未翻译统计仅剩 `SCV`，但该条目在官方中文资源中本就保留为 `SCV`。
- 当前阻塞已经不在 `XMFinal/CommanderTestBench` 内容层，也不再是地图启动层；当前待解的是 chat 命令自动注入链路，解决后即可继续 `Nova` / `Dehaka` 的 smoke 回归。
- 后续仍需把实际同步到游戏目录、启动 `CommanderTestBench`、读取 summary/log 的闭环补完。

## 下一步

1. 继续修 `live-smoke-originalmod-existing-commanders.ps1` 的 chat 注入链路，目标是让 `-tbnova`、`-tbdehaka`、`-tbsmoke` 能稳定送达。
2. 注入链路恢复后，立即执行 `full_units + full_buildings` 默认回归；优先从 `Nova`、`Dehaka` 这类现成指挥官开始。
3. 逐个指挥官跑 `CommanderTestBench`，记录 created/missing/warnings/errors。
4. 若后续出现新增 roster/building 条目，再同步补进导出脚本的中文来源或最终映射。
