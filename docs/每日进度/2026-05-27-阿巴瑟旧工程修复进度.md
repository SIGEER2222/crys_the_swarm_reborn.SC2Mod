# 2026-05-27 阿巴瑟旧工程修复进度

时间：2026-05-27 19:49:49

## 本轮已完成

1. 统一了旧工程里的阿巴瑟入口
   - 将 `AbathurReborn` 启动别名归一到 `Abathur`
   - 已改：
     - `tools/launcher_mpq/Base.SC2Data/GameData/UserData.xml`
     - `tools/launcher_mpq/MapScript.galaxy`
     - `合作指挥官版起义狂潮/Maps/XM/LauncherAuto.SC2Map/Base.SC2Data/GameData/UserData.xml`
     - `合作指挥官版起义狂潮/Maps/XM/LauncherAuto.SC2Map/MapScript.galaxy`

2. 修了 `XMFinal` 里的阿巴瑟启动链
   - 读取 bank 后将 `AbathurReborn` 归一成 `Abathur`
   - 阿巴瑟开局已接回：
     - 指挥官面板初始化
     - 生物质掉落/拾取
     - 开局阿巴瑟编队生成
   - 主改文件：
     - `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146.galaxy`
     - `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146_h.galaxy`

3. 清掉了这轮新增的阿巴瑟脚本错误
   - 之前我补进去的毒巢事件注册在当前 `Launcher` runtime 下拿不到：
     - `SpawnToxicNest`
     - `ToxicNestKillCaster`
   - 这两条注册现已从 `XMFinal.InitTriggers()` 撤掉，避免启动即报错

## 实机验证

已执行：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\sync-all-to-live.ps1 -ReplacePackedLauncher
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\live-launch-and-click-abathur.ps1 -Commander Abathur -MapClick 1 -InitialLoadWaitMs 15000 -EscapeCount 20
```

结果：

- 2026-05-27 19:42 那轮的 `ScriptError` 已定位并清除
  - 当时错误：
    - `SpawnToxicNest` 无效
    - `ToxicNestKillCaster` 无效
- 2026-05-27 19:45 这一轮启动后没有新的 `ScriptError.txt`
- 验证脚本不再报 `script-error`，而是 `timeout`
- 但截图已证明游戏实际进入了地图，且阿巴瑟起始编队已经出现
  - `tmp/sc2-live-verify/tmp_sc2_entry_20260527-194457.png`
  - `tmp/sc2-live-verify/tmp_sc2_after_20260527-194457.png`

## 当前判断

- “阿巴瑟完全不可用”的第一层硬阻塞已经解除：
  - 不再因为阿巴瑟新增触发器直接炸脚本
  - 可以从 Launcher 进入地图
  - 已有阿巴瑟起始单位落地

- 但阿巴瑟还没有彻底收口：
  - 当前进图画面里仍可见凯瑞甘相关 UI/英雄残留
  - 说明旧地图运行时还有一条 `Kerrigan` 旧链路在抢占或混入阿巴瑟场景

## 20:15 补充进展

1. 启动器阿巴瑟选择报错已补防护
   - 根因：`gt_CommanderChange_Func()` 在切到 `Abathur` 时会读取 `CommanderAch.Sound`
   - 旧启动器里该值为空字符串，原逻辑直接 `SoundLink("")`，会报：
     - `请求的声音文件ID未知：''`
   - 现已改成先取 `lv_sound`，非空才播放
   - 已同时落到：
     - `合作指挥官版起义狂潮/Maps/XM/LauncherAuto.SC2Map/MapScript.galaxy`
     - `tools/launcher_mpq/MapScript.galaxy`

2. 已重新同步 `合作指挥官版起义狂潮` 线到游戏目录
   - 使用：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\sync-all-to-live.ps1 -ReplacePackedLauncher
```

3. 验证脚本自身的两处误报已修
   - `scripts/live-verify-abathur.ps1`
   - 已修：
     - 指挥官 portrait 坐标按 `15 + 4` 布局更新
     - 截图时若 SC2 窗口矩形临时失效，不再直接因为 `Bitmap(width,height)` 失败中断
     - 退出阶段若 SC2 已自行关闭，不再把清场流程算成失败

4. 当前实机验证状态
   - 2026-05-27 20:05:14 之后没有产生新的 `ScriptError.txt`
   - 这说明“切到阿巴瑟时因空音效直接炸启动器脚本”这一条，目前没有再复现到新的日志里
   - 但 20:11~20:15 这轮共享 SC2 运行时证据不干净：
     - 最新抓到的 `GameLogs` 启动参数里出现了 `Maps\\XM\\CommanderTestBench.SC2Map`
     - 该路径属于另一条 `原始mod` 测试线，不是本轮 `Launcher.SC2Map` 验证目标
   - 因此这轮截图、bank、GameLogs 不能直接当作“阿巴瑟已完整可玩”的最终证据

## 现阶段结论

- 已闭环：
  - 阿巴瑟入口别名归一
  - `XMFinal` 阿巴瑟开局初始化恢复
  - 毒巢无效运行时注册移除
  - 启动器阿巴瑟空音效脚本错误修复

- 仍待收口：
  - 需要在不受 `原始mod/CommanderTestBench` 干扰的干净 SC2 运行时里，再做一轮 `Launcher.SC2Map -> Abathur -> 地图` 的最终验证
  - 再确认旧工程地图内是否还存在阿巴瑟场景被其他指挥官链路抢占的问题

## 20:18 再次补充

1. 我又补跑了一轮不自动关游戏的验证
   - 使用：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\live-verify-abathur.ps1 -LaunchGame -RestartExisting -CloseGame:$false -Commander Abathur -MapClick 1 -InitialLoadWaitMs 15000 -EscapeCount 0
```

2. 这轮脚本本身成功返回
   - 关键输出：
     - `COMMANDER=Abathur`
     - `COMMANDER_INDEX=15`
     - `MAP_INDEX=1`
     - `LOAD_WAIT_SIGNAL=timeout`
   - 新产物：
     - `tmp/sc2-live-verify/tmp_sc2_before_20260527-201453.png`
     - `tmp/sc2-live-verify/tmp_sc2_selection_20260527-201453.png`
     - `tmp/sc2-live-verify/tmp_sc2_mapselected_20260527-201453.png`
     - `tmp/sc2-live-verify/tmp_sc2_entry_20260527-201453.png`
     - `tmp/sc2-live-verify/tmp_sc2_after_20260527-201453.png`
     - `tmp/sc2-live-verify/tmp_abathur_probe_20260527-201453.txt`
     - `tmp/sc2-live-verify/tmp_abathur_abathur_debug_20260527-201453.json`

3. 这轮得到的新增结论
   - 2026-05-27 20:15 前后的 `GameLogs` 里仍没有新的 `ScriptError.txt`
   - 说明 launcher 侧“选中阿巴瑟即炸脚本”已经稳定消失
   - 但 `C:\Users\22448\Documents\StarCraft II\Banks\CampaignXCore.SC2Bank` 当前 `Ach/Commander` 仍显示 `Raynor`
   - 所以下一步要继续查：
     - 启动器选将是否真的点到了阿巴瑟并写回 bank
     - 或旧工程运行时是否仍有默认指挥官覆盖了选将结果

## 下一步

1. 顺着 `XMFinal` 和所选地图 `MapScript.galaxy` 继续排查 `Kerrigan` 混链
2. 确认阿巴瑟场景里到底是谁在创建/自动选中 `K5Kerrigan`
3. 再单独决定毒巢能力应挂在：
   - 地图层
   - `XMCore` 通用 GP 面板层
   - 还是 `XMAbathur.SC2Mod` 数据层自身
