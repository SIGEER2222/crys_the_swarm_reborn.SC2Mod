# 2026-05-27 官方合作指挥官数据导出修正

时间：2026-05-27

## 本轮目标

按 `游戏数据/官方合作指挥官/commanders/` 现有结构，补全/修正各指挥官的兵种、建筑、英雄与技能相关数据，不改目录结构，优先修导出脚本并整批重导。

## 已完成

1. 修了官方合作指挥官导出脚本
   - 文件：
     - `scripts/sc2/export-official-coop-game-data.py`
   - 这轮新增了三类修正：
     - 把 `command_cards.json` 里的按钮能力摘要直接回填到 `units.json` / `buildings.json` / `heroes.json` / `roster.json` 的 `abilities`
     - 当条目本身缺图标或缺提示时，优先用按钮数据和生产按钮数据兜底
     - 对没有卡面板、但有训练/变异入口的条目，最少补一条生产能力摘要，避免“兵种有了但技能全空”

2. 已整批重导官方合作指挥官数据
   - 命令：

```powershell
python .\scripts\sc2\export-official-coop-game-data.py --repo-root .
```

   - 输出目录：
     - `游戏数据/官方合作指挥官`

3. 已更新的关键输出
   - 全部指挥官的：
     - `roster.json`
     - `units.json`
     - `buildings.json`
     - `heroes.json`
     - `command_cards.json`
     - `README.md`

## 结果

### 结构层

- 现在 `units.json` / `buildings.json` / `heroes.json` 里不再只有单位本体和生产信息
- 每个条目都已带 `abilities`，可直接看到：
  - 按钮名
  - Tooltip
  - 图标
  - `abil_cmd`
  - 按钮所在卡面位置

### 数据完整性

我额外跑了一轮完整性检查，结果：

- `roster.json` 全部条目：
  - 无缺失图标
  - 无缺失 tooltip
  - 无缺失 `abilities`

也就是当前 `游戏数据/官方合作指挥官/commanders/*` 这套导出，已经能直接覆盖“兵种 / 建筑 / 英雄 / 技能”四类基础阅读需求。

## 抽样确认

### Kerrigan

- `commanders/Kerrigan/heroes.json`
  - `K5Kerrigan` 已有：
    - 图标
    - tooltip
    - `abilities`

### Abathur

- `commanders/Abathur/units.json`
  - 单位条目已带 `abilities`
  - 包括进化、主动技能、被动占位等按钮摘要

## 当前仍保留的口径

- 这套数据仍然是“官方合作 Tech / 面板导出视角”
- 不等于“运行时最终可玩映射”
- 如果后面要做：
  - 某个指挥官的实装
  - 某个单位的技能闭环
  - 某个建筑的科技链校验
- 仍应继续对照：
  - `游戏数据/官方合作指挥官/commanders/<Commander>/`
  - `游戏数据/官方合作指挥官/_source-cache/live-casc-export`
  - 目标 Mod 当前实际 runtime

## 下一步建议

1. 先用这套修正后的 `commanders/<Commander>/*.json` 作为单指挥官实现输入
2. 若要继续做某个具体指挥官，优先从：
   - `units.json`
   - `buildings.json`
   - `heroes.json`
   - `command_cards.json`
   - `progression.json`
   开始
3. 如果你要，我下一轮可以直接把某一个指定指挥官再整理成一份“可实装清单”
