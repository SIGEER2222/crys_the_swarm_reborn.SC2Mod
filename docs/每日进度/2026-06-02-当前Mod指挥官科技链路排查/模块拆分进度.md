# 指挥官模块拆分进度

日期：2026-06-02

## 本轮目标

继续把 `XMFinal.SC2Mod` 里的指挥官私有 Catalog 拆回各自模块，保持 `XMFinal` 主要承担统一 runtime、路由和共享框架职责。

## 本轮已完成

### 德哈卡

已把德哈卡的一批私有定义从 `XMFinal.SC2Mod` 抽回 `XMDehaka.SC2Mod`，包括：

- `CommanderData.xml`
- `AbilData.xml`
- `ActorData.xml`
- `BehaviorData.xml`
- `ButtonData.xml`
- `EffectData.xml`
- `LightData.xml`
- `ModelData.xml`
- `MoverData.xml`
- `RequirementData.xml`
- `RequirementNodeData.xml`
- `SoundData.xml`
- `UnitData.xml`
- `UpgradeData.xml`
- `ValidatorData.xml`
- `WeaponData.xml`
- `soundexclusivitydata.xml`

对应操作脚本：

- `scripts/sc2/move-commander-catalog-entries.mjs`

验证结果：

- `node --check scripts/sc2/move-commander-catalog-entries.mjs` 通过。
- 目标文件 XML 解析通过。
- 再跑 dry-run 时，德哈卡目标条目已不再残留在 `XMFinal` 中。

## 当前状态

- `XMFinal` 仍然保留统一 runtime / 名册 / 路由入口。
- 德哈卡模块现在持有自己的关键 Catalog 入口和依赖。
- `XMFinal` 里仍有其他指挥官的私有内容，说明拆分还没结束。

## 下一步建议

优先继续拆残留最多、但边界相对清楚的指挥官分支。

按当前宽松候选计数，优先级大致可以看成：

1. 阿塔尼斯
2. 雷诺
3. 沃拉尊

如果后续要继续机械化推进，建议优先把搬运脚本扩成“按官方指挥官 JSON 生成目标集合”的模式，再批量处理下一位。

