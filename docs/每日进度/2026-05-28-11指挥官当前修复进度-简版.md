# 2026-05-28 11指挥官当前修复进度（简版）

本轮把 `XMFinal` 的运行时依赖头和当前校验口径一起修正了，避免继续出现“文件在盘上，但运行时单位无效”的假象。

## 当前状态

- `XMAbathur`：当前架构校验通过
- `XMAbathurReborn`：当前架构校验通过
- `XMAlarak`：当前架构校验通过
- `XMArtanis`：当前架构校验通过
- `XMFenix`：当前架构校验通过
- `XMKarax`：当前架构校验通过
- `XMKerrigan`：当前架构校验通过
- `XMRaynor`：当前架构校验通过
- `XMVorazun`：当前架构校验通过
- `XMZagara`：当前架构校验通过
- `XMZeratul`：当前架构校验通过

## 本轮做了什么

- 修复了 `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/DocumentHeader`
- 让 `XMFinal` 的二进制依赖头和 `DocumentInfo` 保持一致，补进 11 个目标指挥官模块
- 修正 `scripts/sync-all-to-live.ps1`
  - 同步前自动回写 `XMFinal` 的 `DocumentHeader` 依赖表
- 修正 `scripts/validate-coop-commander-current.ps1`
  - 恢复对正式 runtime 链路的校验
  - 新增对 `XMFinal` `DocumentHeader` 依赖表的检查

## 这轮确认到的重点

- 之前的“单位类型无效”并不全是文件缺失，`XMFinal` 的 header 依赖表本身就过期了
- `XMFinal` 当前 source / live 的 `DocumentHeader` 都已经是 25 条依赖
- 11 个目标指挥官的当前架构校验已经全部通过

## 本轮跑过的验证

- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\sync-all-to-live.ps1 -SkipLauncher`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Abathur`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander AbathurReborn`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Alarak`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Artanis`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Fenix`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Karax`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Kerrigan`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Raynor`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Vorazun`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Zagara`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander Zeratul`

## 剩余风险

- 这轮没有再用 `SC2_x64` 自动进图做 live 烟测；当前结论来自静态验证和目录级同步验证
- 如果后续还要做实机确认，需要用人工直接进图的方式补一次，不再走自动客户端启动
