# 2026-05-26 官方合作指挥官 runtime 生效链对比

## 结论先行

这轮按“实机是否可能生效”的口径重看了一遍，结论是：不能再用“官方主表同 ID 已闭环”直接判断玩法完成。

静态主表只能说明单位、升级、按钮等对象大体存在；runtime 还要看 `XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146.galaxy` 是否真的做了这些事：

1. 是否从 Bank 读取指挥官等级加点和精通点，并实际发放对应升级。
2. 是否创建对应指挥官的技能面板单位、英雄单位或专用 caster。
3. 地图开局队伍、货舱队伍是否直接写死普通单位，从而绕过官方的进化、替换、英雄或精通链。
4. 指挥官是否依赖自己的专用初始化库，而不是通用开局队伍 helper。

当前最明确的 runtime 偏差仍是凯瑞甘：数据里有腾跃虫链和多技能英雄，但 `XMFinal` 开局/货舱 helper 仍直接创建普通跳虫等单位。其他指挥官里，沃拉尊货舱空军分支也有明确疑点；冻结模块不能因为 `XMFinal` 有 caster 初始化就判定完成。

## 通用加点和精通链

`InitializeBase` 开头会先做一层通用发放：

- 读取 `CampaignXCore` Bank。
- 读取当前指挥官的 `AchBit`，按 bit 0 到 7 给 `CommanderAch.Upg[1..8]` 加 1 级。
- 读取 Bank 里的 `"0"` 到 `"5"` 六组精通点，给 `CommanderAch.Poi[1..6]` 加对应等级。
- 然后创建主基地、工人和 `SecondUnit`，最后进入各指挥官分支。

也就是说，多数指挥官的“等级加点/精通”并不是在每个指挥官分支里逐条写死，而是靠 `CommanderAch` 这套 UserData 统一映射。分支里出现的额外处理，才是需要单独盯的 runtime 特例。

当前能直接看到的特例：

| 指挥官 | runtime 特例 | 当前能确认的具体效果 |
|---|---|---|
| 凯瑞甘 | 分支内硬发升级 | 直接给 15 级、凯瑞甘指挥官标记、感染皮肤、英雄坚韧、原始突袭、灵能打击、连锁闪电、冷却缩减、狂怒、能量回复。 |
| 泰凯斯 | 精通点影响亡命徒出场计时 | Bank 第 4 组精通点每点折算 3 秒；创建英雄时用 `90 - 点数*3`，非英雄场景用 `240 - 点数*3`。 |
| 蒙斯克 | 多组精通直接改全局倍率 | 第 1 组每点给普通士兵生成/训练相关倍率加 1%；第 2 组每点给皇家卫队相关倍率加 1%；第 6 组每点给皇家卫队经验加 0.5；第 5 组每点给 caster 能量加 2；第 6 个等级 bit 额外给经验加 50。 |
| 诺娃 | 精通点进粘滞线升级 | 第 2 组精通点传给 `SOAStickyLineUpg`，直接影响诺娃面板线。 |
| 斯台特曼 | 精通点进能量场触发 | 第 3 组精通点传给 `PowerFieldTrigger`；第 8 个等级 bit 会加入超级盖瑞语音响应。 |
| 德哈卡 | 等级 bit 控制额外单位 | 第 1 个等级 bit 为真时，会在德哈卡英雄初始化后调用兄弟单位逻辑。 |

## 一页总览

| 指挥官 | 静态主表状态 | `XMFinal` 初始化 | 开局/货舱 helper | runtime 风险 | 关键判断 |
|---|---|---|---|---|---|
| 阿巴瑟 | 19 同 ID，2 个 Reborn 改名 | 有生物质初始化和面板 | 有专用 helper，Reborn 时会换破坏者、虫群宿主、虫后、利维坦 | 中 | 双阿巴瑟并存下，helper 已处理部分 Reborn 单位，但轻型队伍仍直接给跳虫/蟑螂/刺蛇；重点不是主表缺，而是旧版与 Reborn 链是否被正确选中。 |
| 阿拉纳克 | 10/10 同 ID | 已接 `CoopCasterAlarak` 和 `AlarakCoop` | 有专用 helper，单位大多是塔达林特化 | 中低 | runtime 主入口已接上；后续重点是清理模块混入链和确认虚空辉光舰/航母等扩展单位是否符合预期。 |
| 阿塔尼斯 | 12/12 同 ID | 有亚顿之矛 caster | 有专用 helper，使用艾尔狂热者、龙骑、不朽者、掠夺者等 | 低到中 | 开局队伍基本是阿塔尼斯特化 ID；但货舱 ultimate 直接给航母和风暴战舰，没有像地图开局一样检查风暴战舰解锁。 |
| 德哈卡 | 25/25 同 ID，冻结 | 有 `CasterDehaka`、英雄和德哈卡事件初始化 | 未看到通用开局/货舱 helper | 冻结/专用 | 不能按通用 helper 判定，必须看德哈卡专用库里的吞噬、精华、原始合成和英雄等级链。 |
| 菲尼克斯 | 12/12 同 ID | 有 `SoACasterFenix` 和英雄 | 有专用 helper | 中 | 多数单位是净化者/菲尼克斯特化，但重型和空军直接给普通不朽者、航母；冠军三人组只在 ultimate 且有升级时出现。 |
| 霍纳与汉 | 1 同 ID，2 异 ID，7 缺失，冻结 | 有 Mira/Horner 初始化和 caster | 未看到通用开局/货舱 helper | 高/冻结 | 主表本身还没闭环，不能进入“runtime 已一致”结论；恶火/恶蝠还是 Mira 改名链，多个霍纳舰船缺失。 |
| 凯拉克斯 | 13/13 同 ID | 有亚顿之矛 caster | 有专用 helper | 中 | helper 直接给狂热者、相位哨兵、不朽者、航母、巨像；单位存在不等于太阳能矩阵、修复光束、顶栏和建筑链已按官方生效。 |
| 凯瑞甘 | 9/9 同 ID | 有 caster、英雄和一批硬发升级 | 有专用 helper，但直接写普通单位 | 高 | 已确认数据里有腾跃虫和多技能英雄；但 helper 仍给普通跳虫、刺蛇、虫后、雷兽，实机差异不能靠主表解释。 |
| 蒙斯克 | 25/25 同 ID，冻结 | 有皇帝之影 caster、能量/经验/倍率特例 | 未看到通用开局/货舱 helper | 冻结/专用 | 关键在蒙斯克专用库：士兵武器、皇家卫队经验、统治力、顶栏。`XMFinal` 只证明入口存在。 |
| 诺娃 | 11 同 ID，2 异 ID，2 缺失，冻结 | 有 Nova caster、粘滞线精通、英雄初始化 | 未看到通用开局/货舱 helper | 高/冻结 | 主表仍缺幽灵和自动机炮，兵营/导弹塔是 Nova 改名链；不能因有英雄初始化就判定完成。 |
| 雷诺 | 15/15 同 ID | 有 Raynor caster | 有专用 helper | 中低 | helper 直接给陆战队、医疗兵、劫掠者、坦克、维京、女妖、战列、雷诺突击队；休伯利安/战列终极队伍受战列解锁检查。 |
| 斯台特曼 | 30/30 同 ID，冻结 | 有 Stetmann caster 和能量场触发 | 未看到通用开局/货舱 helper | 冻结/专用 | 主表静态闭环，但真正生效在机械虫群、卫星场、盖瑞/超级盖瑞等专用库，不应只看 `XMFinal`。 |
| 斯托科夫 | 14 同 ID，1 缺失，冻结 | 有 Stukov caster 和全图菌毯 | 未看到通用开局/货舱 helper | 高/冻结 | 仍缺虫后；感染建筑、殖民地民兵、亚历山大号、末日巨兽都要看 Stukov 专用库。 |
| 斯旺 | 9 同 ID，1 异 ID，4 缺失，冻结 | 有激光钻机绑定和 Swann caster | 未看到通用开局/货舱 helper | 高/冻结 | 主表还缺恶蝠、导弹塔、毁灭炮塔、火焰炮塔，大力神为 Swann 改名链；激光钻初始化不能覆盖这些缺口。 |
| 泰凯斯 | 13/13 同 ID，冻结 | 有 Tychus caster、亡命徒 squad 初始化和计时精通 | 未看到通用开局/货舱 helper | 冻结/专用 | 静态主表闭环，但真实完成度取决于酒吧、装备、亡命徒替换、面板按钮与计时链。 |
| 沃拉尊 | 10/10 同 ID | 有 Vorazun caster | 有专用 helper | 中高 | 地图 air 队伍是海盗船/虚空辉光舰/先知，但货舱 air 队伍却是追猎者/黑暗圣堂/不朽者，疑似复制了重型分支。 |
| 扎加拉 | 8/8 同 ID | 有 Zagara caster 和英雄 | 有专用 helper | 低到中 | helper 已使用爆虫、爆蚊、扎加拉腐化者、爆虫、蟑螂、刺蛇、虫后等；轻型队伍使用虫群跳虫，整体比凯瑞甘更贴近特化链。 |
| 泽拉图 | 12/12 同 ID | 有 Zeratul caster 和英雄 | 有专用 helper，基本全是泽拉图特化 ID | 低 | 开局/货舱队伍使用泽拉图狂热者、追猎者、机械哨兵、不朽者、干扰者、虚空阵列等；风险重点转到神器碎片、投影和顶栏，不是普通单位替换。 |

## 高风险项

### 凯瑞甘

当前不能再写“凯瑞甘静态闭环所以暂不用动”。更准确的说法是：凯瑞甘主表对象存在，但 runtime 生效链未闭环。

已确认的具体偏差：

- 数据层有 `KerriganZerglingEvo -> CoopZerglingRaptor -> HotSRaptor`。
- 数据层有 `K5Kerrigan` 多技能按钮和 `HaveK5Fury / HaveK5Cooldowns` 等显示条件。
- `XMFinal` 初始化会硬发一批凯瑞甘英雄升级。
- 但 `KerriganCreateMapStartSquad` 和 `KerriganCreateCargoSquad` 仍直接创建：
  - 轻型：普通跳虫、刺蛇。
  - 重型：刺蛇、虫后、雷兽。
  - 空军：异龙、巢虫领主。
  - 英雄：凯瑞甘英雄、普通跳虫、刺蛇、虫后。

所以用户实测“跳虫还是普通跳虫”是有代码证据支撑的。下一步应优先查训练入口和开局 helper 是否需要在凯瑞甘等级/进化条件满足时替换成腾跃虫，英雄技能则要查实际进场单位、按钮卡布局和等级/需求是否被上层覆盖。

### 沃拉尊

沃拉尊主表是 10/10 同 ID，但货舱 air 分支明显可疑。

- 地图开局 air：海盗船、虚空辉光舰、先知。
- 货舱 air：追猎者、黑暗圣堂武士、不朽者。

这不是“官方主表缺不缺”的问题，而是同一个指挥官的地图开局和货舱队伍不一致。若地图中用到货舱空军队伍，实机就可能拿到完全不是空军的队伍。

### 冻结且主表未闭环的模块

这些模块即使 `XMFinal` 有初始化入口，也不能判定玩法完成：

- 霍纳与汉：7 缺失，2 异 ID。
- 诺娃：2 缺失，2 异 ID。
- 斯托科夫：1 缺失。
- 斯旺：4 缺失，1 异 ID。

它们本轮仍按冻结处理，只记录风险，不建议直接改数据。

## 中风险项

### 阿巴瑟

阿巴瑟不是简单缺数据，而是旧阿巴瑟与 Reborn 并存带来的运行时选择风险。

`XMFinal` helper 已经在 `AbathurReborn` 时替换：

- 破坏者：改用 Reborn 破坏者。
- 虫群宿主：改用合作虫群宿主。
- 虫后：改用合作虫后。
- 利维坦：改用合作利维坦。

但轻型队伍仍直接给普通跳虫、蟑螂、刺蛇；阿巴瑟是否需要更深的官方变异链，要继续看虫群宿主、破坏者、穿刺者、终极进化和生物质是否在 Reborn 分支全部落到正确单位。

### 菲尼克斯

菲尼克斯有 caster、英雄和开局 helper，静态主表闭环，但 helper 里有两个需要复核的点：

- 重型队伍直接给普通不朽者，而不是明确的菲尼克斯/净化者不朽者。
- 空军队伍直接给普通航母。
- 冠军三人组只在有 `FenixChampionClolarionCarrier` 时出现；货舱 helper 没有同样的 ultimate 分支。

这不一定是错误，但不能只用 12/12 同 ID 说明冠军、替换和升级链已经在实机里完整生效。

### 凯拉克斯

凯拉克斯 helper 会直接给狂热者、相位哨兵、不朽者、航母、巨像。这个层面不像凯瑞甘那样明显错给普通跳虫，但凯拉克斯的核心不只在兵种：

- 亚顿之矛 caster 是否完整。
- 修复光束是否实际挂上。
- 太阳能矩阵、顶栏技能和建筑科技是否完整。
- 相位哨兵的接管、能量恢复等升级是否会被精通/等级正确影响。

所以凯拉克斯应标成“主表闭环，但 topbar/修复/太阳能链待实机或专项校验”。

### 阿拉纳克

阿拉纳克现在已经有 `CoopCasterAlarak`、`AlarakCoop` 和专用开局 helper。运行时主入口比早先状态好很多。

需要继续盯的是：

- 模块里前面已经发现过 Karax 混链，虽然清过两轮，仍应按剩余外来链继续扫。
- helper 里的虚空辉光舰、航母是当前扩展队伍，不一定来自官方主表，需要确认这是项目设计还是混入残留。

### 雷诺

雷诺 helper 是标准人族队伍，主表也闭环。当前风险主要是终极分支：

- 有战列解锁时，终极队伍给战列巡航舰和休伯利安。
- 没有战列解锁时，回退到重型队伍。

这说明雷诺比凯瑞甘更像“runtime 已按升级条件分支”，但仍要进图确认休伯利安、佣兵面板、空投/轨道技能是否完整。

### 阿塔尼斯

阿塔尼斯 helper 基本使用艾尔特化单位，风险较低。唯一要记一笔的是：

- 地图 ultimate 会检查风暴战舰解锁。
- 货舱 ultimate 直接给航母和风暴战舰。

如果货舱队伍是调试/奖励用，这可能可接受；如果它代表同等进度队伍，就需要补同样的解锁检查。

## 低风险项

### 扎加拉

扎加拉 helper 已经使用虫群跳虫、爆虫、爆蚊、扎加拉腐化者等特化单位，整体不像凯瑞甘那样明显绕过进化结果。当前风险只剩：

- 虫后仍是普通 `Queen`，是否应替换要按官方扎加拉链复核。
- 扎加拉英雄和群体狂暴、孵化室/爆虫链需要进图或专项脚本验证。

### 泽拉图

泽拉图 helper 基本都在用泽拉图特化 ID，包括狂热者、追猎者、机械哨兵、不朽者、干扰者、虚空阵列、英雄和构造体。当前不应再把风险放在主表单位替换上，而应转到：

- 神器碎片。
- 投影/召唤单位。
- 顶栏技能。
- 泽拉图专用科技和神器等级。

## 专用库优先项

以下指挥官没有在 `XMFinal` 里看到和凯瑞甘同类的通用开局/货舱 helper。它们主要靠自己的专用初始化库、caster、英雄或顶栏逻辑生效：

- 德哈卡
- 霍纳与汉
- 蒙斯克
- 诺娃
- 斯台特曼
- 斯托科夫
- 斯旺
- 泰凯斯

这些指挥官的正确检查口径不是“开局队伍是否创建了正确单位”，而是：

- caster 是否创建。
- 专用库是否初始化。
- 英雄或主建筑是否接到专用系统。
- 精通点是否传入专用变量或触发器。
- 主表缺口是否已经先闭环。

## 后续建议

1. 先修凯瑞甘：把训练入口、开局 helper、货舱 helper、英雄按钮四条链拆开看，不要再用 9/9 主表闭环作为完成判断。
2. 顺手复核沃拉尊货舱 air 分支，判断它是复制错误还是有意设计。
3. 对菲尼克斯、凯拉克斯、阿拉纳克做“helper 直接单位是否为预期设计”的确认，不急着改。
4. 冻结模块继续只记录，不按 runtime 完成判定推进数据修改。
5. 后续所有指挥官进度表建议拆成两列：`静态主表状态` 和 `runtime 生效链状态`，避免再次把静态存在误写成实机一致。
