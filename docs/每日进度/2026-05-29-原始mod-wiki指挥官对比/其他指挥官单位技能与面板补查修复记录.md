# 其他指挥官单位技能与面板补查修复记录

时间：2026-05-29

## 结论

- 已按 18 个合作指挥官重新检查 `原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data`。
- 指挥官面板技能：79 行，缺失 0。
- 单位/建筑攻击与武器链：296 行，运行时攻击/武器风险 0。
- 单位命令卡技能接线：从 51 条风险收敛到 0。
- 这轮仍是静态 Catalog/runtime dependency 审计，不等价于进图逐个按钮实机点击。

## 本轮实际修复

### 扎加拉跳虫

- 不再把爆虫变异加回全局 `Zergling`，避免再次污染凯瑞甘。
- 新增 `ZagaraZergling`，父级为 `Zergling`。
- 扎加拉 roster 改为 `Zergling -> ZagaraZergling`。
- `ZagaraZergling` 当前运行时技能：
  - `stop`
  - `attack`
  - `move`
  - `que1`
  - `BurrowZerglingDown`
  - `MorphToBaneling`
- 额外保留扎加拉特有被动：
  - `MasteryZagaraZerglingDodgeChance`
  - 按钮：`ZagaraVoidCoopZerglingDodge`

### 凯瑞甘跳虫

- 凯瑞甘继续使用原 `Zergling`。
- 当前运行时技能：
  - `stop`
  - `attack`
  - `move`
  - `que1`
  - `BurrowZerglingDown`
- 未挂 `MorphToBaneling` / `MorphZerglingToBaneling`。

### 凯拉克斯太阳锻炉

- `SolarForge` 补回官方太阳锻炉能力：
  - `SolarForgeResearch`
  - `BrokenSolarForge`
- 补回研究按钮：
  - 太阳能效率 1/2/3 级
  - 修复光束额外目标
  - 轨道轰炸升级
  - 太阳能射线升级
- 补回显示/行为相关节点：
  - `SolarForgePreventDestroy`
  - `SolarForgeBeam`
  - `SolarForgeRotate`
  - `SolarForgeBRotate`

### 斯旺科学船

- 命令卡里的无按钮、无名称、无 tooltip 的残留项不再当作主动技能风险。
- 可见按钮按当前运行时别名识别：
  - 战术跳跃：`HyperjumpRSwann`
  - 纳米修复：`NanoRepairSwann`
  - 防御矩阵：`DefensiveMatrixSwann`

### 阿巴瑟飞蛇

- 官方命令卡 `ViperConsumeStructure` 按当前运行时能力 `ViperConsumption` 识别。
- 当前运行时技能链已能解析，不再作为缺接线风险。

## 审计器修正

- `audit-xmfinal-unit-combat-links.mjs` 现在会合并 `CUnit parent` 继承，避免德哈卡、泰凯斯等父模板技能被误判为空。
- 忽略被动、子菜单、取消、`255` 占位、隐藏空按钮等非主动技能。
- 官方命令卡技能只有在官方/当前运行时真实 `AbilArray` 需要时才算缺接线。
- 针对当前运行时别名增加识别：
  - 诺娃 EMP / 狙击
  - 斯旺科学船
  - 扎加拉跳虫
  - 凯拉克斯护盾电池
  - 阿巴瑟飞蛇

## 深层 Catalog 依赖二次收敛

### 审计器误报修正

- `audit-xmfinal-catalog-dependencies.mjs` 新增识别 `CWeaponStrafe`，避免把本地 `InterceptorStetmannBeam` 武器误判为缺失。
- 不再把以下字段当成真实 Catalog 依赖：
  - `ResultNoUnit` / `ResultFailed` 里的错误提示，例如 `CantTargetThatUnit`
  - `BehaviorAlignment` 里的枚举值，例如 `Negative`
  - `AINotifyEffect` 里的 AI 通知字段，例如 `ArtanisMaelstromDamage`、`SuperGaryStetmannPowerTowerOvercharge`
- 将官方/引擎默认引用按 `probable-base-game` 处理：
  - 按钮：`CloakOn`、`RallyCasual`、`RallyEggCasual`
  - 效果哨兵：`InterceptorFate`
  - 官方镜像也未导出定义的目标过滤：`EMP2TargetFilters`、`HunterSeekerLaunchMissileTargetFilters`

### 本地游戏数据补齐

- `ButtonData.xml` 增加按钮别名/补齐：
  - `HHMagneticMinesB` -> 继承 `HHMagneticMines`
  - `HoldFireMengsk` -> 继承 `GhostMengskHoldFire`
  - `LearnDehakaGiveEssence` -> 继承 `LearnDehakaConsume`
  - `MengskStructureArmorResearch` -> 继承 `StructureArmorMengskResearch`
  - `phResearchPrimalMutalisk` -> 继承 `DehakaHydraliskToMutalisk`
  - `PowerTowerCooldownStetmann` -> 继承 `PowerTowerStetmannLevel1`
  - `TrooperMengskInstantTransformResearch` -> 新增本地按钮，使用蒙斯克征召图标，并指向 `InstantTrooperMengskTransform` tooltip/hotkey alias
- `AbilData.xml` 增加能力壳：
  - `DehakaDakrunStructureLearn`
  - `StimpackMengsk`，继承 `Stimpack`
  - `StimpackMengskRedirect`，继承 `StimpackRedirect`
  - `TychusResearchCenterResearch`
- `BehaviorData.xml` 增加隐藏行为壳：
  - `BurrowCracks`
  - `SpawnLocusts`
- `ValidatorData.xml` 增加斯台特曼雷兽钻地冲锋校验：
  - `NotBurrowChargingUltraliskStetmann`，检查 `UltraliskStetmannBurrowCharging`

### 二次收敛结果

```text
XMFINAL_CATALOG_DEP_MISSING: 45 -> 21 -> 0
XMFINAL_CATALOG_DEP_PROFILE_IDS=1213
XMFINAL_CATALOG_DEP_MANUAL_FALLBACK_USED=81
XMFINAL_CATALOG_DEP_METADATA_ONLY=151
XMFINAL_CATALOG_DEP_XM_DEPENDENCY=186
XMFINAL_CATALOG_DEP_OFFICIAL_REFERENCE=826
```

说明：`SpawnLocusts` 在官方 `swarm.sc2mod` 里存在被注释掉的旧 `CBehaviorSpawn` 片段；本轮没有硬搬会主动产蝗虫的行为，只补隐藏行为壳来消除未定义引用。德哈卡虫群宿主实际出蝗虫仍应看 `DehakaSpawnLocustsTargeted` / `DehakaSpawnCreeperTargeted` 的实机表现。

## 验证结果

```text
PROFILE_GENERATOR_OK: rosters/buildings/tech/heroAbilities/unitAbilities
PROFILE_HARD_MISSING=0
ROSTER_BUILDING_CATALOG_MISSING_ENTRIES=0
ABILITY_CATALOG_MISSING_ENTRIES=0
ABILITY_CATALOG_HARD_MISSING_PARTS=0
ABILITY_CATALOG_EXTERNAL_REFERENCED_PARTS=0

XMFINAL_PANEL_SKILL_ROWS=79
XMFINAL_PANEL_SKILL_MISSING_ROWS=0

XMFINAL_UNIT_COMBAT_ROWS=296
XMFINAL_UNIT_COMBAT_RISK=0
XMFINAL_UNIT_COMMAND_CARD_RISK=0

XMFINAL_CATALOG_DEP_MISSING=0
XMFINAL_CATALOG_DEP_XM_DEPENDENCY=186
XMFINAL_CATALOG_DEP_OFFICIAL_REFERENCE=826

XML_PARSE_CHECKED=17
XML_PARSE_BAD=0
Line-limit check passed，当前检查 13 个 Galaxy 文件，最大 961 行
```

## 输出文件

- `docs/每日进度/2026-05-29-原始mod-wiki指挥官对比/panel-skills/xmfinal-panel-skill-audit.md`
- `docs/每日进度/2026-05-29-原始mod-wiki指挥官对比/combat-links/xmfinal-unit-combat-links.md`
- `docs/每日进度/2026-05-29-原始mod-wiki指挥官对比/catalog-after-unit-skill-fix/README.md`

## 剩余风险

- `catalog-after-unit-skill-fix` 的 `missing` 已收敛到 0，但仍有 186 行 XM 依赖模块引用、826 行官方镜像引用；这些是静态可解析依赖假设，不等价于技能实机效果已完全闭环。
- 本轮新增的若干能力/行为是安全壳或别名，目的是消除未定义引用和缺按钮脸；实际费用、冷却、目标过滤、出单位、actor 表现仍需要进图点击确认。
- 需要实机最终确认的仍是：进图后按钮可见性、费用、冷却、目标过滤、actor 表现和实际效果数值。
