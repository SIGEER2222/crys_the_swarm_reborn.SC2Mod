# 2026-05-27 11指挥官当前校验进度（简版）

本轮先没有继续大改指挥官数据本体，而是先把过时的逐指挥官校验脚本对齐到当前 `XMFinal` 统一 runtime 架构，避免旧脚本继续误报。

## 当前状态

- `XMAbathur`：已通过当前架构校验
- `XMAbathurReborn`：已通过当前架构校验
- `XMAlarak`：已通过当前架构校验
- `XMArtanis`：已通过当前架构校验
- `XMFenix`：已通过当前架构校验
- `XMKarax`：已通过当前架构校验
- `XMKerrigan`：已通过当前架构校验
- `XMRaynor`：已通过当前架构校验
- `XMVorazun`：已通过当前架构校验
- `XMZagara`：已通过当前架构校验
- `XMZeratul`：已通过当前架构校验

## 本轮做了什么

- 新增通用校验器：`scripts/validate-coop-commander-current.ps1`
- 将以下旧脚本改成薄包装，统一走当前架构校验：
  - `validate-abathur-port.ps1`
  - `validate-artanis-port.ps1`
  - `validate-fenix-port.ps1`
  - `validate-karax-port.ps1`
  - `validate-kerrigan-port.ps1`
  - `validate-raynor-port.ps1`
  - `validate-vorazun-port.ps1`
  - `validate-zagara-port.ps1`
  - `validate-zeratul-port.ps1`
- `validate-alarak-port.ps1` 现已直接转发到 `validate-alarak-port-fixed.ps1`

## 这轮确认到的真实归属

- `Abathur` 的 `CommanderAch` / 精通 `UserData` 归属在 `XMAbathur.SC2Mod`，不是 `XMCore.SC2Mod`
- `Zeratul` 的精通入口在 `XMCore.SC2Mod`，但精通定义并不只在 `XMZeratul.SC2Mod`，还散在 `XMRaynor.SC2Mod/Base.SC2Data/GameData/commanders/futurecommanders.xml`
- `XMAbathurReborn` 当前已确认保留并归一的核心 AbathurReborn 变种是 `ImpalerAbathur` 与 `RavagerAbathur`

## 本轮跑过的校验

- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-abathur-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-alarak-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-artanis-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-fenix-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-karax-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-kerrigan-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-raynor-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-vorazun-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-zagara-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-zeratul-port.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-coop-commander-current.ps1 -Commander AbathurReborn`

## 下一步

- 如果继续做实装修正，优先顺序建议是：
  1. 再扫一轮各指挥官的 `UserData`/`futurecommanders.xml` 归属，补一份“谁拥有哪些精通/说明文字”的小表
  2. 再做单位/技能/建筑级别的动态验证，而不是继续依赖旧的静态断言
