# XMAbathurReborn 兵种与科技中文清单

- 生成时间：2026-05-28
- 数据来源：`合作指挥官版起义狂潮/Mods/XM/XMAbathurReborn.SC2Mod`，并以官方 `sc2-build-96883-casc-export` 做基础 Catalog fallback。
- 说明：本表按静态命令卡与 Catalog 抽取，可能包含等级/需求锁定项，也可能遗漏运行时动态注入按钮。

## 开局基础单位

- `CommandCenter`：重生阿巴瑟孵化场（`HatcheryAbathurReborn`）
- `SecondUnit`：重生阿巴瑟王虫（`OverlordAbathurReborn`）
- `Worker`：重生阿巴瑟工蜂（`DroneAbathurReborn`）

## 兵种总表

- 利维坦（`Leviathan`）：利维坦
- 刺蛇（`Hydralisk`）：远程攻击单位，可进化为潜伏者。

可攻击地面和空中单位。
- 吞噬者（`Devourer`）：强效远程防空单位，能弱化目标及其周边空中单位。对装甲单位特化，但可击杀/瘫痪任何空中目标。.
- 守护者（`Guardian`）：守护者
- 幼虫（`Larva`）：变异为异虫单位。
- 异龙（`Mutalisk`）：攻击可弹射的飞行单位。

可攻击地面和空中单位。
- 眼虫（`Overseer`）：高级空中侦察单位。 能够维持王虫形态时所控制的单位上限。可以使用超距视界。

侦测单位
- 破坏者（`RavagerAbathur`）：破坏者
- 秽型蟑螂（`RoachVile`）：邪恶蟑螂
- 穿刺者（`ImpalerAbathur`）：穿刺者
- 莽兽（`Brutalisk`）：重型突击巨兽，其体型和力量均远超雷兽。

可以对地和对空
- 虫后（`QueenCoop`）：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。
- 虫群宿主（`SwarmHost`）：攻城单位，能够在扎根或潜地状态下孵化蝗虫进攻。蝗虫持续秒。

蝗虫可以对地。
- 蟑螂（`Roach`）：突击单位。潜地时能快速恢复生命值。

可以对地。
- 重生阿巴瑟工蜂（`DroneAbathurReborn`）：重生阿巴瑟工蜂
- 重生阿巴瑟王虫（`OverlordAbathurReborn`）：重生阿巴瑟王虫
- 飞蛇（`Viper`）：飞行施法者。能够操控战场局势。可使用致盲云雾、绑架和吞噬技能。

## 建筑总表

- 主巢（`Hive`）：孵化场的更高级形态，为异虫提供更多的升级方案和建筑方案。可孵化所有异虫单位和接收采集到的资源。
- 分裂池（`SpawningPool`）：为跳虫提供升级方案。

开启：
- 使孵化场、虫穴和主巢可以孵化虫后
- 使幼虫可以变异为跳虫
- 使工蜂可以变异为脊针爬虫
- 使工蜂可以变异为孢子爬虫
- 刺蛇巢（`HydraliskDen`）：为刺蛇提供升级方案。可以变异为潜伏者巢穴。

开启：
- 使幼虫可以变异为刺蛇
- 大型蟑螂温室（`GreaterRoachWarren`）：强化蟑螂巢穴
- 孢子爬虫（`SporeCrawler`）：防空建筑。

可以对空
侦测单位
- 孵化场（`Hatchery`）：基础建筑，能够孵化所有异虫单位并接收采集到的资源。可以生成菌毯，满足附近异虫建筑的存活需求。可以进化为虫穴。

效果加成：异虫单位在菌毯上的移动速度更快
- 尖塔（`Spire`）：为异虫飞行生物提供升级方案。可以变异为巨型尖塔。

开启：
- 使幼虫可以变异为异龙
- 使幼虫可以变异为腐化者
- 巨型尖塔（`GreaterSpire`）：为异虫飞行生物提供升级方案。

开启：
- 使腐化者可以变异为巢虫领主
- 穿刺者兽穴（`ImpalerDen`）：穿刺者兽穴
- 脊针爬虫（`SpineCrawler`）：对地防御建筑。

可以对地。
- 萃取房（`Extractor`）：建造在瓦斯气泉上，用于采集高能瓦斯。
- 虫穴（`Lair`）：孵化场的高级形态，为异虫提供更多的升级方案和建筑方案。可孵化所有异虫单位和接收采集到的资源。

开启：
- 使王虫可以变异为眼虫
- 虫道网络（`NydusNetwork`）：生产各类盟友可用单位的枢纽建筑。

解锁：
- 巨型坑道虫
- 坑道毁灭者
- 菌毯塔
- 蟑螂温室（`RoachWarren`）：为蟑螂提供升级方案。

开启：
- 使幼虫可以变异为蟑螂
- 使蟑螂可以变异为破坏者
- 进化坑（`EvolutionPit`）：进化坑
- 进化腔（`EvolutionChamber`）：为异虫地面单位和英雄单位提供升级方案。
- 重生阿巴瑟孵化场（`HatcheryAbathurReborn`）：重生阿巴瑟孵化场

## 生产、建造、变异与研究

### 主巢（`Hive`）

- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train4`，成本 150 矿 / -2 补给 / 50 秒；需求 `HaveSpawningPool`；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train1`，成本 150 矿 / -2 补给；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `研究`：充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research7`，成本 60 秒；需求 `LearnPneumatizedCarapaceCoop`；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research2`，成本 50 矿 / 50 气；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化潜地（按钮 `进化潜地`，命令 `LairResearch,Research4`，成本 未静态解析到；说明：使异虫所有的地面单位都能够使用潜地技能。潜地允许单位在地下获得掩护，从而不会遭到攻击。单位潜地后进入隐形状态，只能被侦测单位或侦测效果发现。）
- `研究`：进化腹囊（按钮 `进化腹囊`，命令 `LairResearch,Research3`，成本 100 矿 / 100 气 / 60 秒；说明：使王虫能够运输单位。）

### 刺蛇巢（`HydraliskDen`）

- `研究`：狂暴（按钮 `进化狂暴`，命令 `HydraliskDenResearch,Research6`，成本 120 秒；需求 `LearnFrenzy`；说明：刺蛇的攻击速度提高50%，持续秒。）
- `研究`：进化深槽脊刺（按钮 `进化深槽脊刺`，命令 `HydraliskDenResearch,Research1`，成本 未静态解析到；说明：使刺蛇的攻击射程提高，并强化其远程压制能力。）
- `研究`：进化肌腱扩增（按钮 `进化肌腱扩增`，命令 `HydraliskDenResearch,Research3`，成本 60 秒；需求 `LearnGroovedSpines`；说明：使刺蛇不在菌毯上的移动速度提高25%，射程提高1。）
- `研究`：进化肌腱扩增（按钮 `进化肌腱扩增`，命令 `HydraliskDenResearch,Research4`，成本 未静态解析到；说明：刺蛇在菌毯外的移动速度提高25%。）
- `研究`：进化肌腱扩增（按钮 `进化肌腱扩增`，命令 `HydraliskDenResearch,Research2`，成本 未静态解析到；说明：使刺蛇不在菌毯上的移动速度提高25%，射程提高1。）
- `研究`：进化附属甲壳（按钮 `进化附属甲壳`，命令 `HydraliskDenResearch,Research5`，成本 150 矿 / 150 气 / 90 秒；需求 `LearnAncillaryCarapace`；说明：刺蛇的最大生命值提高20点。）
- `购买强化`：穿刺者兽穴（按钮 `穿刺者兽穴`，命令 `UpgradeToImpalerDen,Execute`，成本 未静态解析到；说明：穿刺者兽穴）

### 吞噬者（`Devourer`）

- `购买强化`：胃囊过载（按钮 `胃囊过载`，命令 `AbathurDevourerStomachOverloadPurchase,Execute`，成本 未静态解析到；说明：该吞噬者获得胃囊过载：移动速度提高10%，攻击速度提高30%。）
- `购买强化`：腐骨腺（按钮 `腐骨腺`，命令 `AbathurDevourerCorrosiveGlandsPurchase,Execute`，成本 未静态解析到；说明：该吞噬者获得腐骨腺：造成的伤害提高10%，并对重甲目标额外造成6点伤害。）
- `购买强化`：酸蚀云囊（按钮 `酸蚀云囊`，命令 `AbathurDevourerAcidCloudSacsPurchase,Execute`，成本 未静态解析到；说明：该吞噬者获得酸蚀云囊：攻击射程+1，远程与溅射伤害提高10%。）

### 大型蟑螂温室（`GreaterRoachWarren`）

- `研究`：应激骨片（按钮 `进化应激骨片`，命令 `RoachWarrenResearch,Research6`，成本 110 秒；需求 `LearnAdaptivePlating`；说明：当蟑螂的生命值低于50%时，获得点额外护甲值。）
- `研究`：氢化胆汁(HotS版)（按钮 `进化氢碘胆汁`，命令 `RoachWarrenResearch,Research5`，成本 110 秒；需求 `LearnHydriodicBile`；说明：蟑螂对轻甲单位造成点额外伤害值。）
- `研究`：进化掘地之爪（按钮 `进化掘地之爪`，命令 `RoachWarrenResearch,Research3`，成本 100 矿 / 100 气；说明：使蟑螂可以在潜地后移动。提高蟑螂在潜地时的生命值恢复速度。）
- `研究`：进化神经胶原重组（按钮 `进化神经胶原重组`，命令 `RoachWarrenResearch,Research2`，成本 未静态解析到；说明：提高蟑螂的移动速度。）

### 孵化场（`Hatchery`）

- `建筑变异`：虫穴（按钮 `变异为虫穴`，命令 `UpgradeToLair,Execute`，成本 500 矿 / 100 气 / 2 补给；说明：孵化场的高级形态，为异虫提供更多的升级方案和建筑方案。可孵化所有异虫单位和接收采集到的资源。

开启：
- 使王虫可以变异为眼虫）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train4`，成本 150 矿 / -2 补给 / 50 秒；需求 `HaveSpawningPool`；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train1`，成本 150 矿 / -2 补给；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `研究`：充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research7`，成本 60 秒；需求 `LearnPneumatizedCarapaceCoop`；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research2`，成本 50 矿 / 50 气；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化潜地（按钮 `进化潜地`，命令 `LairResearch,Research4`，成本 未静态解析到；说明：使异虫所有的地面单位都能够使用潜地技能。潜地允许单位在地下获得掩护，从而不会遭到攻击。单位潜地后进入隐形状态，只能被侦测单位或侦测效果发现。）
- `研究`：进化腹囊（按钮 `进化腹囊`，命令 `LairResearch,Research3`，成本 100 矿 / 100 气 / 60 秒；说明：使王虫能够运输单位。）

### 尖塔（`Spire`）

- `建筑变异`：巨型尖塔（按钮 `变异为巨型尖塔`，命令 `UpgradeToGreaterSpire,Execute`，成本 350 矿 / 350 气；说明：为异虫飞行生物提供升级方案。

开启：
- 使腐化者可以变异为巢虫领主）
- `研究`：加长散射（按钮 `进化加长散射`，命令 `SpireResearch,Research11`，成本 90 秒；需求 `LearnGuardianAttackRangeIncrease`；说明：守护者的攻击射程提高。）
- `研究`：快速再生（按钮 `进化极速再生`，命令 `SpireResearch,Research10`，成本 60 秒；需求 `LearnMutaliskRapidRegeneration`；说明：异龙在脱离战斗后会快速恢复生命值。）
- `研究`：残暴龙爪（按钮 `进化残暴龙爪`，命令 `SpireResearch,Research7`，成本 90 秒；需求 `LearnMutaliskViciousGlave`；说明：异龙的弹射攻击额外增加3次弹跳，并提高弹跳距离。）
- `研究`：腐蚀喷涌（按钮 `进化腐蚀喷涌`，命令 `SpireResearch,Research12`，成本 90 秒；需求 `LearnDevourerAoEDamage`；说明：吞噬者的攻击现在会造成范围性伤害。）
- `研究`：裂骨龙爪（按钮 `进化裂骨龙爪`，命令 `SpireResearch,Research9`，成本 120 秒；需求 `LearnMutaliskSunderingGlave`；说明：异龙对重甲单位造成100%的额外伤害。）
- `研究`：进化飞行生物攻击等级1（按钮 `进化飞行生物攻击等级1`，命令 `SpireResearch,Research1`，成本 未静态解析到；说明：提升所有异虫飞行生物的攻击力。）
- `研究`：进化飞行生物攻击等级2（按钮 `进化飞行生物攻击等级2`，命令 `SpireResearch,Research2`，成本 未静态解析到；说明：进一步提升所有异虫飞行生物的攻击力。）
- `研究`：进化飞行生物攻击等级3（按钮 `进化飞行生物攻击等级3`，命令 `SpireResearch,Research3`，成本 未静态解析到；说明：使所有异虫飞行生物的攻击力最大化。）
- `研究`：进化飞行生物甲壳等级1（按钮 `进化飞行生物甲壳等级1`，命令 `SpireResearch,Research4`，成本 100 矿 / 100 气；说明：提升所有异虫飞行生物的护甲。）
- `研究`：进化飞行生物甲壳等级2（按钮 `进化飞行生物甲壳等级2`，命令 `SpireResearch,Research5`，成本 175 矿 / 175 气；说明：进一步提升所有异虫飞行生物的护甲。）
- `研究`：进化飞行生物甲壳等级3（按钮 `进化飞行生物甲壳等级3`，命令 `SpireResearch,Research6`，成本 250 矿 / 250 气；说明：使所有异虫飞行生物的护甲最大化。）

### 巨型尖塔（`GreaterSpire`）

- `研究`：加长散射（按钮 `进化加长散射`，命令 `SpireResearch,Research11`，成本 90 秒；需求 `LearnGuardianAttackRangeIncrease`；说明：守护者的攻击射程提高。）
- `研究`：快速再生（按钮 `进化极速再生`，命令 `SpireResearch,Research10`，成本 60 秒；需求 `LearnMutaliskRapidRegeneration`；说明：异龙在脱离战斗后会快速恢复生命值。）
- `研究`：残暴龙爪（按钮 `进化残暴龙爪`，命令 `SpireResearch,Research7`，成本 90 秒；需求 `LearnMutaliskViciousGlave`；说明：异龙的弹射攻击额外增加3次弹跳，并提高弹跳距离。）
- `研究`：腐蚀喷涌（按钮 `进化腐蚀喷涌`，命令 `SpireResearch,Research12`，成本 90 秒；需求 `LearnDevourerAoEDamage`；说明：吞噬者的攻击现在会造成范围性伤害。）
- `研究`：裂骨龙爪（按钮 `进化裂骨龙爪`，命令 `SpireResearch,Research9`，成本 120 秒；需求 `LearnMutaliskSunderingGlave`；说明：异龙对重甲单位造成100%的额外伤害。）
- `研究`：进化飞行生物攻击等级1（按钮 `进化飞行生物攻击等级1`，命令 `SpireResearch,Research1`，成本 未静态解析到；说明：提升所有异虫飞行生物的攻击力。）
- `研究`：进化飞行生物攻击等级2（按钮 `进化飞行生物攻击等级2`，命令 `SpireResearch,Research2`，成本 未静态解析到；说明：进一步提升所有异虫飞行生物的攻击力。）
- `研究`：进化飞行生物攻击等级3（按钮 `进化飞行生物攻击等级3`，命令 `SpireResearch,Research3`，成本 未静态解析到；说明：使所有异虫飞行生物的攻击力最大化。）
- `研究`：进化飞行生物甲壳等级1（按钮 `进化飞行生物甲壳等级1`，命令 `SpireResearch,Research4`，成本 100 矿 / 100 气；说明：提升所有异虫飞行生物的护甲。）
- `研究`：进化飞行生物甲壳等级2（按钮 `进化飞行生物甲壳等级2`，命令 `SpireResearch,Research5`，成本 175 矿 / 175 气；说明：进一步提升所有异虫飞行生物的护甲。）
- `研究`：进化飞行生物甲壳等级3（按钮 `进化飞行生物甲壳等级3`，命令 `SpireResearch,Research6`，成本 250 矿 / 250 气；说明：使所有异虫飞行生物的护甲最大化。）

### 幼虫（`Larva`）

- `生产`：刺蛇（按钮 `变异为刺蛇`，命令 `LarvaTrain,Train4`，成本 100 矿 / 50 气 / -2 补给；说明：远程攻击单位，可进化为潜伏者。

可攻击地面和空中单位。）
- `生产`：异龙（按钮 `变异为异龙`，命令 `LarvaTrain,Train5`，成本 100 矿 / 100 气 / -2 补给；说明：攻击可弹射的飞行单位。

可攻击地面和空中单位。）
- `生产`：穿刺者（按钮 `变异为穿刺者`，命令 `LarvaTrainSwarm,Train21`，成本 200 矿 / 100 气 / -3 补给；说明：将幼虫变异为穿刺者。远程攻城单位，可在扎根后从远距离刺穿地面目标。）
- `生产`：虫群宿主（按钮 `变异为虫群宿主`，命令 `LarvaTrainSwarm,Train27`，成本 未静态解析到；说明：将幼虫变异为虫群宿主。攻城单位，可持续孵化蝗虫进行推进。）
- `生产`：虫群宿主（按钮 `变异为虫群宿主`，命令 `LarvaTrain,Train16`，成本 40 秒；需求 `HaveInfestationPit`；说明：攻城单位，通过召唤蝗虫作战。可在地下或移动时攻击。

蝗虫可攻击地面单位。）
- `生产`：虫群宿主（按钮 `变异为虫群宿主`，命令 `LarvaTrainSwarm,Train26`，成本 未静态解析到；说明：将幼虫变异为虫群宿主。攻城单位，可持续孵化蝗虫进行推进。）
- `生产`：虫群宿主（按钮 `变异为虫群宿主`，命令 `LarvaTrain,Train15`，成本 未静态解析到；说明：攻城单位，通过召唤蝗虫作战。可在地下或移动时攻击。

蝗虫可攻击地面单位。）
- `生产`：蟑螂（按钮 `变异为蟑螂`，命令 `LarvaTrain,Train10`，成本 75 矿 / 25 气 / -1 补给；说明：突击单位。潜地时能快速恢复生命值。

可以对地。）
- `生产`：飞蛇（按钮 `变异为飞蛇`，命令 `LarvaTrainSwarm,Train25`，成本 100 矿 / 200 气 / -3 补给；说明：将幼虫变异为飞蛇。飞行施法者，能够使用致盲云雾、绑架和吞噬来操控战场。）
- `生产`：飞蛇（按钮 `变异为飞蛇`，命令 `LarvaTrain,Train13`，成本 100 矿 / 200 气 / -3 补给 / 29 秒；需求 `HaveInfestationPit`；说明：飞行施法者。能够操控战场局势。可使用致盲云雾、绑架和吞噬技能。）

### 异龙（`Mutalisk`）

- `生产`：吞噬者（按钮 `变异为吞噬者`，命令 `MutaliskMorphToDevourer,Train1`，成本 15 秒；需求 `HaveGreaterSpire`；说明：将异龙变异为吞噬者。强效远程防空单位，擅长压制重甲空中目标。）
- `购买强化`：凶暴刃翼（按钮 `凶暴刃翼`，命令 `AbathurMutaliskViciousGlaivePurchase,Execute`，成本 未静态解析到；说明：该飞龙获得凶暴刃翼：远程伤害提高10%，溅射伤害提高15%。）
- `购买强化`：快速再生（按钮 `快速再生`，命令 `AbathurMutaliskRapidRegenerationPurchase,Execute`，成本 未静态解析到；说明：该飞龙获得快速再生：每秒恢复5点生命值。）
- `购买强化`：爆裂刃翼（按钮 `爆裂刃翼`，命令 `AbathurMutaliskExplosiveGlaivePurchase,Execute`，成本 未静态解析到；说明：该飞龙获得爆裂刃翼：对装甲与轻甲目标额外造成8点伤害。）

### 破坏者（`RavagerAbathur`）

- `单位变异`：莽兽（按钮 `进化为莽兽`，命令 `EvolveToBrutaliskRavager,Train1`，成本 500 矿 / 300 气 / 5 秒；需求 `BrutaliskMorphAvailable`；说明：进化为莽兽。）

### 秽型蟑螂（`RoachVile`）

- `单位变异`：破坏者（按钮 `变异为破坏者`，命令 `MorphRoachVileToRavager,Train1`，成本 125 矿 / 75 气 / -3 补给 / 9 秒；需求 `HaveLair`；说明：远程火炮单位。可以使用腐蚀胆汁。

可以对地。）
- `单位变异`：莽兽（按钮 `进化为莽兽`，命令 `EvolveToBrutaliskRoachVile,Train1`，成本 500 矿 / 300 气 / 5 秒；需求 `BrutaliskMorphAvailable`；说明：进化为莽兽。）

### 穿刺者兽穴（`ImpalerDen`）

- `研究`：震地脊刺（按钮 `进化震地脊刺`，命令 `ImpalerDenResearch,Research1`，成本 120 秒；需求 `LearnSeismicSpinesAbathur`；说明：使穿刺者和潜伏者的攻击射程提高3。）

### 虫穴（`Lair`）

- `建筑变异`：主巢（按钮 `变异为主巢`，命令 `UpgradeToHive,Execute`，成本 700 矿 / 250 气 / 2 补给；说明：孵化场的更高级形态，为异虫提供更多的升级方案和建筑方案。可孵化所有异虫单位和接收采集到的资源。）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train4`，成本 150 矿 / -2 补给 / 50 秒；需求 `HaveSpawningPool`；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train1`，成本 150 矿 / -2 补给；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `研究`：充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research7`，成本 60 秒；需求 `LearnPneumatizedCarapaceCoop`；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research2`，成本 50 矿 / 50 气；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化潜地（按钮 `进化潜地`，命令 `LairResearch,Research4`，成本 未静态解析到；说明：使异虫所有的地面单位都能够使用潜地技能。潜地允许单位在地下获得掩护，从而不会遭到攻击。单位潜地后进入隐形状态，只能被侦测单位或侦测效果发现。）
- `研究`：进化腹囊（按钮 `进化腹囊`，命令 `LairResearch,Research3`，成本 100 矿 / 100 气 / 60 秒；说明：使王虫能够运输单位。）

### 虫群宿主（`SwarmHost`）

- `单位变异`：莽兽（按钮 `进化为莽兽`，命令 `EvolveToBrutaliskSwarmHost,Train1`，成本 500 矿 / 300 气 / 5 秒；需求 `BrutaliskMorphAvailable`；说明：进化为莽兽。）
- `购买强化`：加压腺体（按钮 `加压腺体`，命令 `AbathurHostPressurizedGlandsPurchase,Execute`，成本 未静态解析到；说明：该宿主获得加压腺体：视野+2，攻击射程+2。）
- `购买强化`：快速孵化（按钮 `快速孵化`，命令 `AbathurHostRapidIncubationPurchase,Execute`，成本 未静态解析到；说明：该宿主获得快速孵化：自身攻击速度提高25%，每秒额外回复1点生命值。）
- `购买强化`：蝗群提速（按钮 `蝗群提速`，命令 `AbathurHostLocustSpeedPurchase,Execute`，成本 未静态解析到；说明：该宿主获得蝗群提速：移动速度和攻击速度提高15%。）

### 蟑螂（`Roach`）

- `单位变异`：破坏者（按钮 `变异为破坏者`，命令 `MorphRoachToRavager,Train1`，成本 125 矿 / 75 气 / -3 补给 / 9 秒；需求 `HaveLair`；说明：远程火炮单位。可以使用腐蚀胆汁。

可以对地。）
- `单位变异`：莽兽（按钮 `进化为莽兽`，命令 `EvolveToBrutaliskRoach,Train1`，成本 500 矿 / 300 气 / 5 秒；需求 `BrutaliskMorphAvailable`；说明：进化为莽兽。）

### 蟑螂温室（`RoachWarren`）

- `研究`：应激骨片（按钮 `进化应激骨片`，命令 `RoachWarrenResearch,Research6`，成本 110 秒；需求 `LearnAdaptivePlating`；说明：当蟑螂的生命值低于50%时，获得点额外护甲值。）
- `研究`：强酸胆汁（按钮 `进化强酸胆汁`，命令 `RoachWarrenResearch,Research8`，成本 120 秒；需求 `LearnCorrosiveBileDamageIncrease`；说明：破坏者的腐蚀胆汁伤害提高点。）
- `研究`：氢化胆汁(HotS版)（按钮 `进化氢碘胆汁`，命令 `RoachWarrenResearch,Research5`，成本 110 秒；需求 `LearnHydriodicBile`；说明：蟑螂对轻甲单位造成点额外伤害值。）
- `研究`：肿胀胆管（按钮 `进化肿胀胆管`，命令 `RoachWarrenResearch,Research7`，成本 90 秒；需求 `LearnCorrosiveBileRadiusIncrease`；说明：破坏者的腐蚀胆汁的命中范围增大%。）
- `研究`：进化掘地之爪（按钮 `进化掘地之爪`，命令 `RoachWarrenResearch,Research3`，成本 100 矿 / 100 气；说明：使蟑螂可以在潜地后移动。提高蟑螂在潜地时的生命值恢复速度。）
- `研究`：进化神经胶原重组（按钮 `进化神经胶原重组`，命令 `RoachWarrenResearch,Research2`，成本 未静态解析到；说明：提高蟑螂的移动速度。）

### 进化坑（`EvolutionPit`）

- `研究`：联网甲壳（按钮 `进化网状化甲壳`，命令 `EvolutionPitResearch,Research3`，成本 60 秒；需求 `LearnAbathurNetworkedCarapace`；说明：激活甲壳后，所有邻近的友方单位也会获得甲壳效果。）
- `研究`：蝗虫可对空（按钮 `研究加压腺体`，命令 `EvolutionPitResearch,Research2`，成本 90 秒；需求 `LearnAbathurLocustAirAttack`；说明：虫群宿主的蝗虫可以对空。）
- `研究`：阿巴瑟改良版剧毒巢穴（按钮 `研究改良版剧毒巢穴`，命令 `EvolutionPitResearch,Research4`，成本 60 秒；需求 `LearnAbathurImprovedToxicNest`；说明：你获得剧毒巢穴2次使用次数，并且冷却时间缩短5秒。）
- `研究`：阿巴瑟隐秘剧毒巢穴（按钮 `隐匿毒巢`，命令 `EvolutionPitResearch,Research1`，成本 90 秒；需求 `LearnAbathurHiddenToxicNest`；说明：隐匿毒巢）

### 进化腔（`EvolutionChamber`）

- `研究`：孵化腔（按钮 `进化孵化腔`，命令 `evolutionchamberresearch,Research11`，成本 60 秒；需求 `LearnHatcheryDoubleQueue`；说明：允许孵化场、虫穴和主巢同时生出两只虫后。）
- `研究`：生物机械哺液（按钮 `进化生物机械哺液`，命令 `evolutionchamberresearch,Research12`，成本 60 秒；需求 `LearnBioMechanicalTransfusion`；说明：虫后的速效哺液治疗量提高10点，现在可以对机械单位和建筑使用。）
- `研究`：进化喷射攻击等级1（按钮 `进化喷射攻击等级1`，命令 `evolutionchamberresearch,Research7`，成本 未静态解析到；说明：提升所有异虫地面远程单位的攻击力。）
- `研究`：进化喷射攻击等级2（按钮 `进化喷射攻击等级2`，命令 `evolutionchamberresearch,Research8`，成本 未静态解析到；说明：进一步提升所有异虫地面远程单位的攻击力。）
- `研究`：进化喷射攻击等级3（按钮 `进化喷射攻击等级3`，命令 `evolutionchamberresearch,Research9`，成本 未静态解析到；说明：使所有异虫地面远程单位的攻击力最大化。）
- `研究`：进化地面单位甲壳等级1（按钮 `进化地面单位甲壳等级1`，命令 `evolutionchamberresearch,Research4`，成本 100 矿 / 100 气；说明：提升所有异虫地面单位的护甲。）
- `研究`：进化地面单位甲壳等级2（按钮 `进化地面单位甲壳等级2`，命令 `evolutionchamberresearch,Research5`，成本 175 矿 / 175 气；说明：进一步提升所有异虫地面单位的护甲。）
- `研究`：进化地面单位甲壳等级3（按钮 `进化地面单位甲壳等级3`，命令 `evolutionchamberresearch,Research6`，成本 250 矿 / 250 气；说明：使所有异虫地面单位的护甲最大化。）
- `研究`：进化近战攻击等级1（按钮 `进化近战攻击等级1`，命令 `evolutionchamberresearch,Research1`，成本 未静态解析到；说明：提升所有异虫单位的近战攻击力。）
- `研究`：进化近战攻击等级2（按钮 `进化近战攻击等级2`，命令 `evolutionchamberresearch,Research2`，成本 未静态解析到；说明：进一步提升所有异虫单位的近战攻击力。）
- `研究`：进化近战攻击等级3（按钮 `进化近战攻击等级3`，命令 `evolutionchamberresearch,Research3`，成本 未静态解析到；说明：使所有异虫单位的近战攻击力最大化。）

### 重生阿巴瑟孵化场（`HatcheryAbathurReborn`）

- `建筑变异`：虫穴（按钮 `变异为虫穴`，命令 `UpgradeToLair,Execute`，成本 500 矿 / 100 气 / 2 补给；说明：孵化场的高级形态，为异虫提供更多的升级方案和建筑方案。可孵化所有异虫单位和接收采集到的资源。

开启：
- 使王虫可以变异为眼虫）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train4`，成本 150 矿 / -2 补给 / 50 秒；需求 `HaveSpawningPool`；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `生产`：虫后（按钮 `孵化虫后`，命令 `TrainQueen,Train1`，成本 150 矿 / -2 补给；说明：防御型单位。能使用孵化幼虫、产下菌毯肿瘤和哺液技能。在菌毯上的移动速度大幅提升。

可以对地和对空。）
- `研究`：充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research7`，成本 60 秒；需求 `LearnPneumatizedCarapaceCoop`；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化充气甲壳（按钮 `进化充气甲壳`，命令 `LairResearch,Research2`，成本 50 矿 / 50 气；说明：提高王虫和眼虫的移动速度。）
- `研究`：进化潜地（按钮 `进化潜地`，命令 `LairResearch,Research4`，成本 未静态解析到；说明：使异虫所有的地面单位都能够使用潜地技能。潜地允许单位在地下获得掩护，从而不会遭到攻击。单位潜地后进入隐形状态，只能被侦测单位或侦测效果发现。）
- `研究`：进化腹囊（按钮 `进化腹囊`，命令 `LairResearch,Research3`，成本 100 矿 / 100 气 / 60 秒；说明：使王虫能够运输单位。）

### 重生阿巴瑟工蜂（`DroneAbathurReborn`）

- `建造`：分裂池（按钮 `变异为分裂池`，命令 `ZergBuild,Build4`，成本 250 矿；说明：为跳虫提供升级方案。

开启：
- 使孵化场、虫穴和主巢可以孵化虫后
- 使幼虫可以变异为跳虫
- 使工蜂可以变异为脊针爬虫
- 使工蜂可以变异为孢子爬虫）
- `建造`：刺蛇巢（按钮 `变异为刺蛇巢`，命令 `ZergBuild,Build6`，成本 150 矿 / 100 气；说明：为刺蛇提供升级方案。可以变异为潜伏者巢穴。

开启：
- 使幼虫可以变异为刺蛇）
- `建造`：孢子爬虫（按钮 `变异为孢子爬虫`，命令 `ZergBuild,Build16`，成本 125 矿；说明：防空建筑。

可以对空
侦测单位）
- `建造`：孵化场（按钮 `变异为孵化场`，命令 `ZergBuild,Build1`，成本 350 矿 / 2 补给；说明：基础建筑，能够孵化所有异虫单位并接收采集到的资源。可以生成菌毯，满足附近异虫建筑的存活需求。可以进化为虫穴。

效果加成：异虫单位在菌毯上的移动速度更快）
- `建造`：尖塔（按钮 `变异为尖塔`，命令 `ZergBuild,Build7`，成本 250 矿 / 200 气；说明：为异虫飞行生物提供升级方案。可以变异为巨型尖塔。

开启：
- 使幼虫可以变异为异龙
- 使幼虫可以变异为腐化者）
- `建造`：脊针爬虫（按钮 `变异为脊针爬虫`，命令 `ZergBuild,Build15`，成本 150 矿；说明：对地防御建筑。

可以对地。）
- `建造`：萃取房（按钮 `变异为萃取房`，命令 `ZergBuild,Build3`，成本 75 矿；说明：建造在瓦斯气泉上，用于采集高能瓦斯。）
- `建造`：虫道网络（按钮 `变异为虫道网络`，命令 `ZergBuild,Build10`，成本 200 矿 / 200 气；说明：生产各类盟友可用单位的枢纽建筑。

解锁：
- 巨型坑道虫
- 坑道毁灭者
- 菌毯塔）
- `建造`：蟑螂温室（按钮 `变异为蟑螂温室`，命令 `ZergBuild,Build14`，成本 200 矿；说明：为蟑螂提供升级方案。

开启：
- 使幼虫可以变异为蟑螂
- 使蟑螂可以变异为破坏者）
- `建造`：进化腔（按钮 `变异为进化腔`，命令 `ZergBuild,Build5`，成本 125 矿；说明：为异虫地面单位和英雄单位提供升级方案。）

### 飞蛇（`Viper`）

- `购买强化`：增殖脑池（按钮 `增殖脑池`，命令 `AbathurCasterBrainPoolPurchase,Execute`，成本 未静态解析到；说明：该单位获得增殖脑池：能量恢复速度+1，并立即恢复25点能量。）
- `购买强化`：拟态隐膜（按钮 `拟态隐膜`，命令 `AbathurCasterMimeticMembranePurchase,Execute`，成本 未静态解析到；说明：该单位获得拟态隐膜：脱离战斗约5秒后进入隐匿，攻击或受创后会暂时显形。）
- `购买强化`：血肉转炉（按钮 `血肉转炉`，命令 `AbathurCasterFleshReactorPurchase,Execute`，成本 未静态解析到；说明：该单位获得血肉转炉：最大能量+50，能量恢复速度额外+0.5，并立即恢复15点能量。）
- `购买强化`：远距神经束（按钮 `远距神经束`，命令 `AbathurCasterNeuralTethersPurchase,Execute`，成本 未静态解析到；说明：该单位获得远距神经束：视野+2。施法距离强化将改为直接作用技能本体，而非错误地改武器射程。）

## 指挥官解锁项

- 生物质收集（`AbathurRebornCommander`）：敌方非建筑单位死亡后掉落生物质，阿巴瑟单位靠近后可以拾取。
- 毒巢网络（`AbathurRebornBiomassLifeLeech`）：解锁毒巢投放，并启用毒巢充能与伏击链路。
- 共生体适应（`AbathurRebornImprovedToxicNest`）：共生体相关单位和效果解锁到正式强化状态。
- 进化腔升级（`AbathurRebornImprovedMend`）：开放阿巴瑟核心科技与关键升级链。
- 残暴进化（`AbathurRebornSymbiote`）：允许地面单位通过生物质阈值进化为残暴兽。
- 空中终极进化（`AbathurRebornEnableSymbiote`）：允许空中单位通过生物质阈值进化为利维坦。
- 变异甲壳（`AbathurRebornMorphTimeCostReduced`）：强化高生物质单位的生存与持续作战能力。
- 巢群强化（`AbathurRebornToxicNestIcreasedBiomass`）：完成阿巴瑟最终强化，巩固全军综合战力。

## 精通

- 剧毒巢穴伤害（`MasteryAbathurRebornToxicNestDamageAndRespawn`）：提高剧毒巢穴伤害。
- 愈合治疗持续时间（`MasteryAbathurRebornMendHeal`）：延长愈合的持续治疗时间。
- 共生体技能强化（`MasteryAbathurRebornSymbioteCarapace`）：强化共生体护盾与刺击伤害。
- 双倍生物质几率（`MasteryAbathurRebornDoubleBiomass`）：提高敌人掉落双倍生物质的几率。
- 剧毒巢穴最大充能数和冷却时间（`MasteryAbathurRebornToxicNestCharge`）：提高剧毒巢穴最大充能数，并缩短充能冷却。
- 建筑变异和研究时间（`MasteryAbathurRebornTechFastBuild`）：缩短建筑变异和科技研究时间。

## 备注

- 单位与建筑总表按阿巴瑟重生命令链递归抽取，不是把整个 `XMAbathurReborn` 模块的全部对象原样平铺。
- `购买强化` 和 `研究` 都保留了原始按钮名与 `AbilCmd`，后续要回查 XML 或做自动化校验时更方便。
- 如果后面还要补“精确成本已应用指挥官默认升级后的最终值”，建议在这份脚本上继续叠加 `AbathurRebornCommander` 的 Upgrade Effect 求值。
