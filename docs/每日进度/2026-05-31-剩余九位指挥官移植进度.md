# 2026-05-31 剩余指挥官移植进度

## 当前范围

本轮继续处理剩余指挥官的移植收口：

- 阿拉纳克
- 阿塔尼斯
- 菲尼克斯
- 凯拉克斯
- 雷诺
- 斯旺
- 沃拉尊
- 扎加拉
- 泽拉图（已收口，保留参考）

## 今日完成

### 雷诺

- 已把官方按钮 `CommanderRaynor*` 和 `SetBunkerRallyPoint` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMRaynor.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 最新官方 vs mod 对比结果：`buttons / abilities / commander_perks` 已全部归零，可以先按“收口完成”处理。

### 阿拉纳克

- 已把官方按钮 `CommanderAlarak*` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMAlarak.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 最新官方 vs mod 对比结果：`buttons` 已归零，`abilities` 也已归零，当前只剩 `commander_perks: 15/21`。

### 阿塔尼斯

- 已把官方按钮 `CommanderArtanis*` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMArtanis.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMArtanis.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 最新官方 vs mod 对比结果：`buttons` 已归零，当前仍有 `units: 1/25`、`abilities: 1/41` 两条异常项和 `commander_perks: 16/21`。
- 这两条异常项里，`AlarakSupplicantWarpTrainCreator` 需要继续核对是否属于跨指挥官误判，不要直接当成阿塔尼斯真实缺项。

### 菲尼克斯

- 已把官方按钮 `CommanderFenix*` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMFenix.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMFenix.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 最新官方 vs mod 对比结果：`buttons / units / abilities / commander_perks` 已全部归零，Fenix 这一位可以先按“收口完成”处理。

### 凯拉克斯

- 已把官方按钮 `CommanderKarax*`、`CommanderKaraxUnlockSOAPassiveOrbitalStrike` 和 `ShieldBatteryRecharge` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMKarax.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMKarax.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 最新官方 vs mod 对比结果：`buttons / abilities / commander_perks` 已全部归零，Karax 这一位可以先按“收口完成”处理。

### 斯旺

- 已把官方按钮 `CommanderSwann*` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMSwann.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`。
- 已把对应中文 `Button/Name` / `Button/Tooltip` 补进 `合作指挥官版起义狂潮/Mods/XM/XMSwann.SC2Mod/zhCN.SC2Data/LocalizedData/GameStrings.txt`。
- 已在 `scripts/sc2/compare-official-commanders-vs-mod.mjs` 补 Swann 的别名归并，把 `SICocoonInfestedWraith` / `SIWraith` / `SIMorphtoInfestedWraith` 对齐到 `WraithSwann`。
- 最新官方 vs mod 对比结果：`units / abilities / buttons / commander_perks` 已全部归零，Swann 这一位可以按“收口完成”处理。

### 泽拉图（已收口参考）

- 已把官方进度按钮 `CommanderZeratulLevel01` 到 `CommanderZeratulLevel15` 的 `CButton` 定义补进 `合作指挥官版起义狂潮/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/GameData/ButtonData.xml`，并补齐对应中文 `Button/Name` / `Button/Tooltip`。
- 已在 `scripts/sc2/compare-official-commanders-vs-mod.mjs` 增加 Zeratul 的别名归并，把官方 progression 旧名对齐到当前 mod 里的真实实现名。
- 最新官方 vs mod 对比结果：`units/buildings/upgrades/abilities/buttons/prestiges/commander_perks` 全部归零，Zeratul 这条已经可以暂时视为收口。

### 对比脚本已修正

- 已给 `scripts/sc2/compare-official-commanders-vs-mod.mjs` 加上 progression 别名层，先把官方 `perks/masteries` 的 `id` 对齐到 mod 中实际出现的 `button / upgrade` 名称，再做差异统计。
- 已额外补了一批已确认的手工别名，优先覆盖雷诺、沃拉尊、扎加拉、阿塔尼斯、菲尼克斯、凯拉克斯和斯旺这几位最容易误报的条目，并过滤掉了 `255` 这类纯数字占位。
- 最新重跑后，`Alarak`、`Artanis`、`Raynor`、`Fenix`、`Karax`、`Swann`、`Vorazun`、`Zagara`、`Zeratul` 已经收口为 `0`。
- 现在这份报告更适合拿来当“真缺项清单”，后续继续盯剩余还没归零的指挥官分支。

## 当前验证入口

- 最新差异报告：
  - `docs/每日进度/2026-05-31-官方指挥官与mod差异对比/official-vs-mod-by-commander.md`
  - `docs/每日进度/2026-05-31-官方指挥官与mod差异对比/official-vs-mod-by-commander.json`

## 下一步

1. 继续按 compare / runtime 链路收口剩余还没归零的指挥官分支。
2. 保持 `DocumentInfo` / `DocumentHeader` 不动，后续只在需要时再考虑同步到 live 目录。
