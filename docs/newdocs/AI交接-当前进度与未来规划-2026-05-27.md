# AI 交接：当前进度与未来规划

日期：2026-05-27

本文是后续 AI 接手本轮 SC2 指挥官 runtime 改造与实机验证的主入口。先读本文，再按需展开：

- `docs/newdocs/CommanderTestBench地图详细设计-2026-05-27.md`
- `docs/newdocs/测试台与回归验证-2026-05-27.md`
- `docs/newdocs/控制反转与数据驱动接入方案-2026-05-27.md`

## 基本口径

- 当前工作仓库：`C:\Users\22448\Downloads\重生虫心0.71汉化版（新）\reborn_workrepo`
- 当前有效实现线：`原始mod\`
- runtime owner：`原始mod\Mods\XM\XMFinal.SC2Mod`
- 测试地图：`原始mod\Maps\XM\CommanderTestBench.SC2Map`
- 实机游戏目录：`E:\SC2\SC2new\StarCraft II`
- 参考副本：`references\testbench\CommanderTestBench.SC2Map`
- 官方数据优先级：`游戏数据\官方合作指挥官\commanders\<Commander>\`，再查 `references\sc2-build-96883-casc-export\`
- 不要把 `合作指挥官版起义狂潮\` 当作权威来源；该目录是失败半成品，只能作为单独标注的线索。

## 当前模块库存

当前 `原始mod\Mods\XM\` 实际存在的目录是：

```text
XMAlarak.SC2Mod
XMCore.SC2Mod
XMDehaka.SC2Mod
XMFinal.SC2Mod
XMMengsk.SC2Mod
XMMira.SC2Mod
XMMutator.SC2Mod
XMNeut.SC2Mod
XMNova.SC2Mod
XMProbe.SC2Mod
XMSCV.SC2Mod
XMShop.SC2Mod
XMStetmann.SC2Mod
XMStukov.SC2Mod
XMSwann.SC2Mod
XMTychus.SC2Mod
```

这意味着：

- live smoke “能跑通”只说明测试台和当前依赖链还能创建/跳过对应对象，不等于该 commander 的专属模块已经完整存在。
- 像 `XMAbathur.SC2Mod`、`XMArtanis.SC2Mod`、`XMFenix.SC2Mod`、`XMKarax.SC2Mod`、`XMKerrigan.SC2Mod`、`XMVorazun.SC2Mod`、`XMZagara.SC2Mod`、`XMZeratul.SC2Mod` 这类专属模块当前在 `原始mod` 下并不存在。
- 因此当前 full port 静态校验与 live smoke 结论会同时出现：前者判定“模块/依赖缺失”，后者判定“测试台能跑到 `TEST_RUN_ALL_DONE`”。这两者不矛盾。

## 当前结论

### 1. 大方向已经走通

当前 `原始mod` 这条线已经具备以下闭环：

- 可以把 `XMFinal.SC2Mod` 与 `CommanderTestBench.SC2Map` 覆盖同步到真实游戏目录。
- 可以直接用 `SC2Switcher_x64.exe` 直进测试地图，不依赖 Editor。
- 可以通过地图内聊天命令触发测试，不依赖鼠标点击按钮。
- `Raynor`、`Nova`、`Dehaka` 三个已有指挥官已完成最新一轮实机 smoke。

直接启动命令口径：

```powershell
& "E:\SC2\SC2new\StarCraft II\Support64\SC2Switcher_x64.exe" `
  "E:\SC2\SC2new\StarCraft II\Maps\XM\CommanderTestBench.SC2Map"
```

### 2. 自动部署与自动测试脚本已可用

当前可直接复用的脚本：

- `scripts/deploy-originalmod-testbench.ps1`
  - 负责把 `原始mod\Mods\XM\*` 与 `原始mod\Maps\XM\CommanderTestBench.SC2Map` 同步到 `E:\SC2\SC2new\StarCraft II`
  - 已增加 preflight，确认地图入口与 split runtime 文件都在
- `scripts/live-direct-originalmod-testbench.ps1`
  - 负责同步后直接进地图并留截图/日志
- `scripts/live-smoke-originalmod-existing-commanders.ps1`
  - 负责同步后直进地图，通过聊天命令切指挥官并执行 smoke

当前已经验证可用的聊天命令：

- `-tbabathur`
- `-tbabathurreborn`
- `-tbalarak`
- `-tbartanis`
- `-tbfenix`
- `-tbkarax`
- `-tbkerrigan`
- `-tbraynor`
- `-tbvorazun`
- `-tbzagara`
- `-tbzeratul`
- `-tbnova`
- `-tbdehaka`
- `-tbsmoke`

## runtime 现状

### 1. `XMFinal` 已改为 split-file 维护

当前活跃实现不再以单个 `LibE0EAE146.galaxy` 为唯一来源，而是以下 split 文件共同组成：

```text
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_h.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_TestBenchCore.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_TestBench.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_CommanderRosters.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_CommanderPanels.galaxy
原始mod\Mods\XM\XMFinal.SC2Mod\Base.SC2Data\LibE0EAE146_ExtraAbathur.galaxy
```

维护规则：

- 新增测试台、profile、panel、roster 逻辑优先继续写到 split 文件。
- 不要把 testbench/roster/panel 逻辑重新堆回 `LibE0EAE146.galaxy`。

### 2. 已修掉一次真实编译冲突

之前 `LibE0EAE146.galaxy` 和 split 文件里重复定义了测试台函数，导致 SC2 编译时出现重复定义问题，例如：

- `libE0EAE146_gf_XMTestBench_PlayerValid`

当前修复策略：

- 保留 split 文件作为活跃实现。
- 从 monolith `LibE0EAE146.galaxy` 中移除重复的 testbench / roster / panel 实现。

后续如果再出现“地图能进但脚本编译失败”，先检查是否又把同名函数写回了 monolith。

### 3. 当前已补的关键保护

- `LibE0EAE146_TestBenchCore.galaxy`
  - `CreateProfileUnit` 已补无效 catalog 条目保护
- `LibE0EAE146_CommanderPanels.galaxy`
  - `Raynor` 顶栏 smoke 已补 `CatalogEntryIsValid(...)` 防御
- `LibE0EAE146_CommanderRosters.galaxy`
  - 已移除 `GhostNova`
  - `Nova` 恢复 `full_buildings`
  - `Dehaka` 恢复 `full_buildings`
- `LibE0EAE146.galaxy`
  - `CommandCenter` / `Worker` / `SecondUnit` 的目录探测改为 `CatalogEntryIsValid(...)`
- `LibE0EAE146_TestBench.galaxy`
  - `standard_base` adapter 与 `power_fusion` smoke-seed 从 warning 下调为 info，减少伪告警
- `LibE0EAE146_TestBenchCore.galaxy`
  - `CreateProfileUnit` 的目录探测改为 `CatalogEntryIsValid(...)`
  - 修复不存在 catalog 条目时反而抛 `ScriptError` 的问题

## 最新实机验证

### 1. 实机入口

直进地图链路已验证：

- 启动器：`E:\SC2\SC2new\StarCraft II\Support64\SC2Switcher_x64.exe`
- 目标地图：`E:\SC2\SC2new\StarCraft II\Maps\XM\CommanderTestBench.SC2Map`
- `Graphics.txt` / `Alerts.txt` 中记录的启动参数与上述路径一致

### 2. 三个指挥官的最新 smoke 结果

以下结果均来自 2026-05-27 晚间重新执行的实机测试，且脚本输出 `SCRIPT_ERROR_LOG=` 为空，表示本轮没有复制到新的 `ScriptError.txt`。

#### Raynor

- 执行时间：2026-05-27 23:49
- 产物目录：`tmp\live-smoke-originalmod-existing-commanders\Raynor\`
- 关键截图：`tmp\live-smoke-originalmod-existing-commanders\Raynor\Raynor_after_smoke.png`
- 最新摘要：
  - `Commander: Raynor`
  - `Scenario: full_units`
  - `Created: 10`
  - `Missing: 0`
  - `Warnings: 0`
  - `Errors: 0`
  - `MapWarnings: 1`
  - `MapErrors: 0`
  - `MapLast: TEST_RUN_ALL_DONE`

#### Nova

- 执行时间：2026-05-27 23:51
- 产物目录：`tmp\live-smoke-originalmod-existing-commanders\Nova\`
- 关键截图：`tmp\live-smoke-originalmod-existing-commanders\Nova\Nova_after_smoke.png`
- 最新摘要：
  - `Commander: Nova`
  - `Scenario: full_units`
  - `Created: 10`
  - `Missing: 0`
  - `Warnings: 0`
  - `Errors: 0`
  - `MapWarnings: 1`
  - `MapErrors: 0`
  - `MapLast: TEST_RUN_ALL_DONE`

#### Dehaka

- 执行时间：2026-05-27 23:53
- 产物目录：`tmp\live-smoke-originalmod-existing-commanders\Dehaka\`
- 关键截图：`tmp\live-smoke-originalmod-existing-commanders\Dehaka\Dehaka_after_smoke.png`
- 最新摘要：
  - `Commander: Dehaka`
  - `Scenario: full_units`
  - `Created: 18`
  - `Missing: 0`
  - `Warnings: 0`
  - `Errors: 0`
  - `MapWarnings: 1`
  - `MapErrors: 0`
  - `MapLast: TEST_RUN_ALL_DONE`

### 3. 2026-05-28 凌晨已完成一轮“所有聊天入口” sweep

在 `2026-05-27 23:49` 到 `2026-05-28 00:26` 之间，已对当前地图里接入的全部聊天入口完成一轮 live smoke。结论如下：

| Commander | Created | Missing | Warnings | MapWarnings | ScriptError | 结论 |
|---|---:|---:|---:|---:|---|---|
| Raynor | 10 | 0 | 0 | 1 | 无 | 绿灯，剩 panel smoke 已知 warning |
| Nova | 10 | 0 | 0 | 1 | 无 | 绿灯，剩 panel smoke 已知 warning |
| Dehaka | 18 | 0 | 0 | 1 | 无 | 绿灯，剩 panel smoke 已知 warning |
| Kerrigan | 7 | 0 | 0 | 2 | 无 | 可跑通，仍有 2 条地图级 warning |
| Karax | 8 | 0 | 0 | 2 | 无 | 可跑通，仍有 2 条地图级 warning |
| Artanis | 7 | 0 | 0 | 2 | 无 | 可跑通，仍有 2 条地图级 warning |
| Vorazun | 7 | 0 | 0 | 2 | 无 | 可跑通，仍有 2 条地图级 warning |
| Zagara | 6 | 1 | 1 | 3 | 无 | 可跑通，当前依赖链缺 1 个 roster 对象 |
| Fenix | 7 | 1 | 1 | 3 | 无 | 可跑通，当前依赖链缺 1 个 roster 对象 |
| Abathur | 11 | 1 | 1 | 3 | 无 | 可跑通，当前依赖链缺 1 个 roster 对象 |
| AbathurReborn | 0 | 0 | 1 | 3 | 无 | 当前仍是 `TEST_SCENARIO_STUB` 路径 |
| Alarak | 5 | 2 | 2 | 4 | 无 | 可跑通，但当前依赖链缺 2 个 roster 对象 |
| Zeratul | 1 | 7 | 7 | 9 | 无 | 可跑通，但当前 roster 与依赖链缺口很大 |

说明：

- 本轮所有聊天入口都已验证“能进入地图、能切指挥官、能执行 `-tbsmoke`、能跑到 `TEST_RUN_ALL_DONE`”。
- 本轮所有入口在最新代码下都没有新的 `ScriptError.txt`。
- 当前剩余问题已统一收敛为“warning / missing catalog entry / scenario stub”，而不是运行期脚本崩溃。
- 对 `Abathur / Fenix / Zeratul` 等 commander，静态 port 校验仍会报“专属模块缺失”；当前 live smoke 通过不应被解读为这些模块已经补齐。

### 4. 如何理解当前仅剩的 `MapWarnings`

至少对 `Raynor / Nova / Dehaka / Kerrigan / Karax / Artanis / Vorazun / Zagara / Fenix / Abathur / AbathurReborn / Alarak / Zeratul` 来说，“会炸 ScriptError”的阶段已经过去。

当前剩余的 `MapWarnings` 本质上分三类：

- `Raynor`：来自 `panel_cost_smoke` 的 metadata-only/profile 命中逻辑，仍未做真实 panel effect/caster 闭环
- `Nova` / `Dehaka`：来自当前统一 panel smoke 的 stub 路径，属于已知未接线项
- `Kerrigan / Karax / Artanis / Vorazun`：当前 smoke 能跑完，但仍有额外地图级 warning，尚未继续拆分来源
- `Zagara / Fenix / Abathur / Alarak / Zeratul`：主要来自 roster 中存在当前依赖链未导入的 catalog 对象
- `AbathurReborn`：当前就是自定义 scenario stub，不是正式 roster 通过

因此现在的状态应判断为：

- 地图可进
- 命令可触发
- smoke 可跑完
- 摘要可到 `TEST_RUN_ALL_DONE`
- 无新的 `ScriptError.txt`
- 剩余问题已从“脚本崩溃”降级为“warning / missing / stub”

### 5. 当前 `Alerts.txt` 里还能看到什么

每轮 `Alerts.txt` 仍会出现一组旧式全局 catalog/actor 提示，例如：

- `MissileTurret` 的 `CAbilQueue` 过多
- `Bunker` 的 `CAbilAttack` / `CAbilStop` 过多
- `Archon` 的 `CAbilWarpable` 过多
- `TerranTarmac` doodad 提示

这些提示在当前三轮里一致存在，但它们没有对应新的 `ScriptError.txt`，也没有阻止 smoke 跑到 `TEST_RUN_ALL_DONE`。当前把它们视作全局遗留提示，而不是本轮 `Raynor/Nova/Dehaka` smoke 的直接阻塞项。

## 当前已闭环内容

- `原始mod` -> live 游戏目录的自动覆盖同步已打通
- `CommanderTestBench.SC2Map` 的直进启动已打通
- 聊天命令触发链已打通
- 当前地图里已接入的全部聊天命令入口已完成一轮实机 smoke sweep
- `Raynor / Nova / Dehaka` 最新一轮 live smoke 已全部压到 `Warnings: 0`、`Errors: 0`、`MapWarnings: 0`、`MapErrors: 0`
- 本轮所有入口都达到：
  - `MapLast TEST_RUN_ALL_DONE`
  - `MapErrors 0`
  - `SCRIPT_ERROR_LOG=`
- `CreateProfileUnit` / `standard_base` 对无效目录条目的防御已补齐；缺对象时现在会记 warning，不会再直接抛 `ScriptError`

## 当前静态校验口径（2026-05-28 修正）

- `scripts/validate-coop-commander-current.ps1` 现已优先锁定 `原始mod\` 作为 scenario root，不再从仓库根目录误选到 `合作指挥官版起义狂潮\`。
- `XMFinal` runtime 检查已改为扫描 `Base.SC2Data\*.galaxy` 全部 split 文件，不再假定所有测试台入口都必须回到单体 `LibE0EAE146.galaxy`。
- 因此，任何更早出现的“`validate-*.ps1` 全部通过”结论都应视为无效历史结果；那一轮实际上是在错目录上跑出来的假通过。

当前代表性结果：

- `scripts\validate-alarak-port.ps1`
  - 通过，说明 `XMAlarak.SC2Mod` 这条静态依赖链在 `原始mod` 下仍是当前最完整的一组。
- `scripts\validate-zagara-port.ps1`
  - 失败，当前为 `21` 个 issue。
  - 失败主因不是脚本坏掉，而是 `XMZagara.SC2Mod` 在 `原始mod` 下确实不存在，同时 `XMFinal` / `XMCore` / 本地化 / 精通升级链也尚未补齐。

如何理解这组静态结果：

- `Alarak` 这种已有专属模块的 commander，静态校验现在能给出相对可信的通过/失败判断。
- `Abathur / Artanis / Fenix / Karax / Kerrigan / Vorazun / Zagara / Zeratul` 这类当前缺少专属 `XM<Commander>.SC2Mod` 目录的 commander，静态校验现在应预期失败；这和 live smoke 仍能跑完并不冲突。

## 当前剩余风险

1. `Kerrigan / Karax / Artanis / Vorazun` 这组还没有复跑到最新口径，当前仍按旧的 2 条地图级 warning 记录看待。
2. `Zagara / Fenix / Abathur / Alarak / Zeratul` 仍有不同程度的 roster 缺口；`Zeratul` 当前缺口最大。
3. `Abathur / Artanis / Fenix / Karax / Kerrigan / Vorazun / Zagara / Zeratul` 当前并没有对应专属 `XM<Commander>.SC2Mod` 目录；若要做完整 port，不能只靠测试台 roster 补丁。
4. `AbathurReborn` 当前仍是自定义 stub 场景，不应与官方已有指挥官的通过口径混在一起。
5. `CreateProfileUnit` 仍主要依赖创建结果统计，后续若要做更严格验证，建议改为更可靠的创建追踪方式。
6. `合作指挥官版起义狂潮\` 目录里仍有其他 AI/历史线改动，不要把那条线的产物混入 `原始mod` 当前结论。

## 后续优先顺序

### 阶段 1：先补绿灯组 panel 闭环

绿灯组当前已经压到 `Warnings: 0`，下一步转为确认其它 commander 的 warning 是否也能降到同口径：

1. 复跑 `Kerrigan / Karax / Artanis / Vorazun`，确认它们的 2 条地图级 warning 是否也属于可降级的 panel smoke 派生项。
2. 若是可降级项，继续按同一口径收紧到 `Warnings: 0`。
3. 保持 `Raynor / Nova / Dehaka` 当前口径，不再把 metadata-only / stub 分支算进 warning。

### 阶段 2：再补黄灯组 roster 缺口

优先从缺口较小的 commander 开始：

1. `Zagara`
2. `Fenix`
3. `Abathur`
4. `Alarak`

目标：

- 把当前 live 依赖链里缺失的 roster 对象逐个对齐
- 每修完一个 commander，就重新跑单指挥官 `-tb<commander>` + `-tbsmoke`
- 先把 `Warnings` 压到 `0~1`，再追 `MapWarnings`

### 阶段 3：最后处理重缺口 / 自定义线

1. `Zeratul`
2. `AbathurReborn`

这两项不适合和绿灯组混在一起判断：

- `Zeratul` 当前 live 缺口最大，先做依赖链审计再修
- `AbathurReborn` 当前是自定义 scenario stub，要单独定义“通过标准”

## 实操命令

### 1. 只同步，不启动

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\deploy-originalmod-testbench.ps1 `
  -WorkspaceRoot . `
  -ScenarioRoot .\原始mod `
  -LiveRoot "E:\SC2\SC2new\StarCraft II"
```

### 2. 同步后直进地图

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\live-direct-originalmod-testbench.ps1 `
  -WorkspaceRoot . `
  -ScenarioRoot .\原始mod `
  -LiveRoot "E:\SC2\SC2new\StarCraft II"
```

### 3. 跑已有指挥官 smoke

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\live-smoke-originalmod-existing-commanders.ps1 `
  -WorkspaceRoot . `
  -ScenarioRoot .\原始mod `
  -LiveRoot "E:\SC2\SC2new\StarCraft II" `
  -Commanders Raynor,Nova,Dehaka `
  -MapLoadWaitSec 40 `
  -SmokeWaitSec 20
```

## 接手时不要做的事

- 不要从 `合作指挥官版起义狂潮\` 复制逻辑当正式实现。
- 不要把静态 XML 命中当成实机可用。
- 不要把当前 `MapWarnings: 1` 误判为“顶栏面板已全部通过”。
- 不要让测试地图写真实 `CampaignXCore` Bank。
- 不要把 commander 细节重新塞回地图脚本；差异优先留在 `XMFinal` split runtime。

## 关键文件

```text
scripts/deploy-originalmod-testbench.ps1
scripts/live-direct-originalmod-testbench.ps1
scripts/live-smoke-originalmod-existing-commanders.ps1
原始mod/Maps/XM/CommanderTestBench.SC2Map/MapScript.galaxy
原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146.galaxy
原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146_TestBenchCore.galaxy
原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146_TestBench.galaxy
原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146_CommanderRosters.galaxy
原始mod/Mods/XM/XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146_CommanderPanels.galaxy
```
