# Alarak 移植进度

生成日期：2026-05-22

## 当前阶段

第一阶段目标：把已有 `XMAlarak.SC2Mod` 从“已有模块但未进候选”推进到“工作区可选择、标准基地可初始化、关键地图有分支、验证脚本可检查”的状态。

## 阶段 1：模块审计结果

### 已存在模块

```text
合作指挥官版起义狂潮/Mods/XM/XMAlarak.SC2Mod
```

### 已存在 GameData 文件

```text
AbilData.xml
AccumulatorData.xml
ActorData.xml
BehaviorData.xml
ButtonData.xml
EffectData.xml
ModData.xml
ModelData.xml
MoverData.xml
SoundData.xml
UnitData.xml
UpgradeData.xml
WeaponData.xml
```

### 已发现核心单位

```text
AlarakCoop
AlarakRushPlaceholder
```

`AlarakCoop` 已有英雄命令卡，包含：

```text
AlarakACDeadlyCharge
AlarakKnockbackAlarak
SoulAbsorption
AlarakEmpower
AlarakDestructionWaveDistance
AlarakLightningStrikes
AlarakAreaDamageUpgrade
AlarakAttackStun
```

### 已发现面板基础

`XMCore.SC2Mod/Base.SC2Data/Lib67C0F0E7.galaxy` 中已经存在：

```text
lib67C0F0E7_gf_CU_GPInitAlarak
AlarakCasterPanelTemplate
AlarakGlobalCommandPanelTemplate
UI_AlarakCastingPanel.SC2Cutscene
```

说明 Alarak 的顶部面板框架已经在 XMCore 里预置，不需要从零写面板初始化。

### 当前缺口

- `XMCore CommanderAch` 没有 `Alarak`。
- `LauncherAuto CommanderPreset` 没有 `Alarak`。
- `XMFinal` 标准初始化暂未确认有 `Alarak` 分支。
- 关键地图脚本暂未统一补 `Alarak` 分支。
- 尚未建立 `validate-alarak-port.ps1`。
- 尚未确认 Alarak 的标准基地单位应使用哪些：第一阶段建议先用 `Nexus / Probe / Pylon`，英雄由 `XMFinal` 创建 `AlarakCoop`。

## 工作记录

### 2026-05-22 阶段 1

- 审计了 `XMAlarak.SC2Mod` 目录结构。
- 确认模块中已有 Alarak 英雄单位 `AlarakCoop`。
- 确认 `AlarakCoop` 英雄本体命令卡存在。
- 确认 `XMCore` 已有 `CU_GPInitAlarak` 和 Alarak 专属顶部面板模板。
- 确认下一步应从接入 `CommanderAch / LauncherAuto / XMFinal / 地图分支 / 验证脚本` 开始，而不是重建模块。

### 2026-05-22 阶段 2

- 新增 `scripts/validate-alarak-port.ps1`，并先运行失败基线，确认缺少 `CommanderAch/Alarak`、Launcher 候选、`XMFinal` 初始化和关键地图分支。
- 在 `XMCore.SC2Mod/Base.SC2Data/GameData/UserData.xml` 中新增 `CommanderAch/Alarak`。
- `CommanderAch/Alarak` 第一阶段采用保守星灵开局：`Nexus / Probe / Pylon`。
- 在 `LauncherAuto.SC2Map` 中把 `CommanderPreset` 扩展到 11 个候选，并追加 `Alarak`。
- `gv_commanderNum` 从 `9` 调整为 `10`，即索引 `0~10` 共 11 个候选。
- 新增 Alarak Launcher 本地化文本和头像引用。
- 在 `XMFinal.SC2Mod/Base.SC2Data/LibE0EAE146.galaxy` 中新增 `Alarak` 控制台皮肤分支。
- 在标准基地初始化中创建 `AlarakRushPlaceholder` 作为顶部面板 caster，调用 `CU_GPInit(1, "Alarak", ...)` 并创建 `AlarakCoop` 英雄。
- 为 10 张关键地图加入 Alarak 分支：`traynor01`、`ttosh03b`、`tvalerian01`、`thanson01`、`thorner02`、`thorner03`、`thorner05s`、`ttychus02`、`ttychus04`、`ttychus05`。
- 关键地图分支第一阶段使用保守星灵单位：`Zealot`、`Stalker`、`Immortal`、`Colossus`、`VoidRay`，无基地图使用 `AlarakCoop` 绑定剧情变量。
- `validate-alarak-port.ps1` 已通过。
- `LibE0EAE146.galaxy` 与 `LauncherAuto.SC2Map/MapScript.galaxy` 括号平衡检查通过。

## 当前已知缺口

- live 游戏使用的 `E:\SC2\SC2new\StarCraft II\Maps\XM\Launcher.SC2Map` 是单文件地图包，工作区 `LauncherAuto.SC2Map` 变更尚未打包进 live Launcher。
- `CommanderAch/Alarak` 使用的是保守星灵通用开局，尚未确认是否应改为塔达林专属 Nexus/Probe/Pylon 变体。
- 地图分支暂用通用星灵单位，后续应替换为 Supplicant/Slayer/Ascendant/Vanguard/Wrathwalker/DeathFleet 等 Alarak 特色单位。
- `AlarakRushPlaceholder` 作为面板 caster 已接入，但顶部面板按钮实机效果尚未验证。
- 英雄死亡/复活闭环尚未专项验证。
- `XMAlarak.SC2Mod` 当前文件体量较小，可能不是完整官方 Alarak 数据，需要后续进行官方 starcoop 数据补导入。

### 2026-05-22 阶段 3

- 新增 `scripts/validate-alarak-tech-chain.ps1`，用于检查 Alarak 核心单位、建筑、技能、按钮、升级和英雄命令卡。
- 首次运行科技链验证时确认缺口：塔达林核心单位/建筑、死亡舰队技能、部分升级对象不完整。
- 新增 `scripts/import-official-alarak-data.ps1`。
- 从官方 `references/official-casc-export/mods/starcoop/starcoop.sc2mod/base.sc2data/gamedata` 导入 Alarak 相关 catalog 对象到 `XMAlarak.SC2Mod`。
- 导入对象数：`4982`。
- 导入摘要：`references/official-alarak-import-summary.tsv`。
- 当前已确认的核心英雄/面板对象：
  - `AlarakCoop`
  - `AlarakRushPlaceholder`
  - `AlarakACDeadlyCharge`
  - `AlarakKnockback`
  - `AlarakEmpower`
  - `AlarakACSummonDeathfleet`
  - `AlarakStructureOvercharge`
- 当前已确认的塔达林/阿拉纳克相关单位：
  - `AlarakSupplicantWarpTrainCreator`
  - `HighTemplarTaldarim`
  - `ImmortalTaldarim`
  - `ColossusTaldarim`
  - `CarrierTaldarim`
  - `SOAMothership`
- 当前已确认的基础星灵建筑：
  - `Nexus`
  - `Probe`
  - `Pylon`
  - `Gateway`
  - `WarpGate`
  - `CyberneticsCore`
  - `TwilightCouncil`
  - `RoboticsFacility`
  - `Stargate`
  - `Forge`
- 已根据实际官方 ID 修正科技链验证脚本：
  - 英雄击退技能实际为 `AlarakKnockback`，不是 `AlarakKnockbackAlarak`。
  - 闪电升级实际对象为 `AlarakSupplicantSacrificeLightningStrikes`，不是 `AlarakLightningStrikes`。
  - 探测/浩劫链路当前以 `Havoc` 相关被动、行为和按钮存在，但未确认存在独立 `Havoc` 单位 ID，因此验证暂不要求独立 `Havoc` 单位。
- 重新运行：
  - `scripts/validate-alarak-tech-chain.ps1` 通过。
  - `scripts/validate-alarak-port.ps1` 通过。
- 修复 `LauncherAuto.SC2Map/MapScript.galaxy` 中 `gv_commanderNum` 回退为 `8` 的问题，重新设为 `10`。

## 阶段 3 后仍需备注的风险

- `XMAlarak.SC2Mod` 现在导入了大量官方对象，但尚未实机确认所有依赖都由当前 Mod 依赖链加载。
- 地图分支仍使用通用星灵单位，尚未切换到完整塔达林单位体系。
- `AlarakCoop` 的死亡/复活逻辑引用中可见 `KerriganRevive`，这可能是官方复用链路或导入依赖表现，实机需重点观察。
- 顶部面板已具备 `AlarakGlobalCommandPanelTemplate` 和 `AlarakRushPlaceholder` caster，但按钮是否完整显示、死亡舰队是否可释放仍需实机验证。
- 部分单位 ID 与俗称不一致，例如 Supplicant 训练链使用 `AlarakSupplicantWarpTrainCreator`，Ascendant 对应 `HighTemplarTaldarim`，Vanguard/Wrathwalker 对应 `ImmortalTaldarim`/`ColossusTaldarim`，后续地图分支替换时必须使用实际 catalog ID。
