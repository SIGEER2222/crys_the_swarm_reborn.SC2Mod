# 阿巴瑟当前状态

日期：2026-05-23

## 2026-05-28 补充口径

- 当前请以 `docs/每日进度/2026-05-28-Abathur-Alarak-Kerrigan-Zeratul完成确认与兵种建筑面板.md` 为最新交接口径。
- `Abathur` 已再次通过当前严格静态校验；现阶段应视为“静态链路已闭环，待统一 live 复验”。
- 这页后续内容可继续作为历史背景和细节说明，但不要再把阿巴瑟当作“尚未接完”的指挥官处理。

## 已完成

### 独立模组与数据
- `XMAbathur.SC2Mod` 已创建并配置完整。
- 包含完整的毒巢（ToxicNest）单位定义：
  - `ToxicNest` - 毒巢单位
  - `ToxicNestBurrowed` - 潜伏毒巢单位
- 包含毒巢相关能力：
  - `SpawnToxicNest` - 投放毒巢
  - `ToxicNestExplode` - 毒巢爆炸
  - `BurrowToxicNestDown` - 毒巢潜入
- 包含毒巢相关升级：
  - `AbathurImprovedToxicNest` - 强化毒巢
  - `AbathurToxicNestIcreasedBiomass` - 毒巢生物质增强
  - `AbathurToxicNestRespawnTalent` - 毒巢复活天赋
  - `AbathurHiddenToxicNest` - 隐藏毒巢

### 面板现状
- 当前使用通用的 `ZergGlobalCommandPanelTemplate`
- 只初始化 2 个按钮
- 官方数据中**没有独立的 `AbathurGlobalCommandPanelTemplate`**
- 这是阿巴瑟的正常设计，不是缺陷（阿巴瑟不像德哈卡那样需要顶部主动技能）

### 运行时
- `XMFinal` 中阿巴瑟初始化已创建 `CoopCasterAbathur`
- `CU_GPInitAbathur` 已接入面板系统
- **生物质触发器已实现**：
  - `libE0EAE146_gt_AbathurBiomassDrop` - 生物质掉落
  - `libE0EAE146_gt_AbathurBiomassPickup` - 生物质拾取

### 地图适配
- `ttychus03` 等特殊地图的占位 Overlord 分支已被正式 helper 调用替换
- `XMFinal` 共享依赖的二分结果见 [XMFinal依赖二分-2026-05-23.md](./Abathur/XMFinal依赖二分-2026-05-23.md)

### XMFinal 元数据
- 已核对原始 `XMFinal.SC2Mod/DocumentInfo`：原始共享入口不直接依赖 `XMCore.SC2Mod`。
- 当前 `XMFinal.SC2Mod/DocumentInfo` 与 `DocumentHeader` 已恢复为 14 个活动依赖，不包含 `XMCore` 和后加指挥官模组。
- `XMCore` 仍然是公共底库，但当前地图链路通过 `XMAlarak.SC2Mod -> XMCore.SC2Mod` 间接加载它。
- 2026-05-23 22:40 已用 `thanson03b.SC2Map` direct-map smoke 验证可进图，`MAP_ENTRY_SIGNAL=alerts`。

## 待完善

### 毒巢触发器缺失 ❌

**问题**: 官方数据中存在以下触发器名称，但实际代码未实现：
- `CM_Abathur_ToxicNest` - 毒巢主触发
- `CM_Abathur_ToxicNestDeathFailsafe` - 毒巢死亡安全触发
- `CM_MasteryAbathurToxicNestRespawn` - 毒巢复活大师精通

**影响**: 毒巢的放置和爆炸逻辑可能无法正常工作

**解决方案**: 需要从官方数据中提取这些触发器的实现代码，并接入到 `XMFinal` 或 `XMCore` 的阿巴瑟初始化流程中。

### 生物质逻辑待实机验证 ⚠️

虽然触发器代码已实现，但需要确认：
- 掉落、拾取、叠层在 live 中是否稳定
- 属性增益是否完整兑现
- 终极进化阈值和触发链是否完全闭环

### 单位与科技链待验证 ⚠️

需要验证：
- 基础训练链
- 科技建筑点击
- 升级发放
- 地图特殊分支是否都落到阿巴瑟逻辑

## 当前状态评估

可以确认的是：
- ✅ 阿巴瑟已经不是"只有名字"的状态
- ⚠️ 静态数据和运行时识别已落仓，但 `XMAbathur.SC2Mod` 重新挂到 `XMFinal` 后仍需单独 live 验证
- ✅ `XMFinal` 当前可启动元数据已恢复到无 `XMCore` 直依赖的 14 依赖状态，并已通过一次 direct-map smoke
- ✅ 生物质触发器已实现并启用
- ✅ 毒巢的单位、能力和升级数据已完整
- ❌ **毒巢的触发器逻辑缺失，需要补充实现**

## 建议后续顺序

1. **最高优先级**: 继续做“单个后加指挥官模组挂回 `XMFinal`”的候选级验证，不要再把 `XMCore` 当成 `XMFinal` 默认直依赖
2. **高优先级**: 从官方数据提取毒巢触发器代码并接入
3. **中优先级**: 用标准基地图确认生物质逻辑
4. **中优先级**: 用一张无基地图确认 helper 给兵
5. **低优先级**: 实机验证毒巢、修复和终极进化

## 历史资料

旧稿已移动到 `archive/`：
- `ABATHUR_HANDOFF_2026-05-22.md`
- `阿巴瑟阶段成果清单-2026-05-22.md`
