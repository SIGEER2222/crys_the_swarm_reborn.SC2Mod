# Zagara 2026-05-23 初始盘点

日期：2026-05-23

## 进度记录

- `XMCore` 已接好 `CommanderAch/Zagara` 的 8 个升级和 6 个精通映射。
- `XMAlarak` 已补齐 Zagara 精通需要的 upgrade / effect / behavior 对象。
- `XMFinal` 已有 `Zagara` runtime 分支和基地初始化接线。
- `XMFinal` 已补上 `ZagaraCreateMapStartSquad` / `ZagaraCreateCargoSquad`，并接入一批特殊地图分支。
- `tools/launcher_mpq` 与 `LauncherAuto.SC2Map` 已同步加入 `Zagara` 候选。
- 现在剩下的主要工作是 live 打包验证、技能实机确认和数值回归。

## 已存在资源

### 官方参考

- `references/official-casc-export/mods/starcoop/starcoop.sc2mod/base.sc2data/gamedata/commanderdata.xml` 里有官方 `CCommander id="Zagara"`，并指向 `PlayerCommanders;ZergZagara`。
- 官方导出里还能直接看到 `ZagaraVoidCoop`、`ZagaraReviveCocoon`、`BileLauncherZagara` 等核心对象。

### XMCore

- Zagara 的分类、触发器、预设值、本地化字符串已经存在。
- `Base.SC2Data/UI/Layout/Coop_UnitStatus.SC2Layout` 里有 `ZagaraCoopStatusFrameTemplate`。
- `Base.SC2Data/GameData/CommanderData.xml` 里有 `ConsoleZerg_Zagara`。
- `UnitData.xml` 里有 `ZerglingZagaraACGluescreenDummy`、`QueenZagaraACGluescreenDummy`、`OverseerZagaraACGluescreenDummy`。

### XMAlarak

这里有大量 Zagara 的完整 gameplay 对象，但更像“共享导入资源”，不是正式 owner：

- `ZagaraVoidCoop`
- `ZagaraVoidCoopBurrowed`
- `ZagaraReviveCocoon`
- `ZagaraVoidCoopBanelingBarrage`
- `ZagaraVoidCoopSpawnHunterKillers`
- `ZagaraVoidCoopMassFrenzy`
- `ZagaraVoidCoopMassRoachDrop`
- `ZagaraVoidCoopTransfusionWave`
- `ZagaraVoidCoopDeepTunnel`
- `ZagaraVoidCoopNydusWorm`
- `ZagaraVoidCoopBurrow` / `Unburrow`
- `CommanderPrestigeZagaraZagara`
- `CommanderPrestigeZagaraMaxSupply`
- `CommanderPrestigeZagaraCorruptorsAberrations`
- `CommanderPrestigeZagaraZagaraMastery`

对应的 model / weapon / effect / requirement / card layout 也都已经在场。

### XMFinal / Maps

- `XMFinal.SC2Mod` 已有 `Zagara` runtime 分支。
- `XMFinal.SC2Mod` 现在额外负责 `Zagara` 的通用地图给兵 helper。
- 已补 `thorner03`、`thanson02`、`traynor01`、`thorner05s`、`ttychus05`、`ttosh03b`、`ttychus04`、`ttychus03`、`tvalerian01` 的 map-start 覆盖。
- 已补 `thanson01`、`thorner02`、`ttychus02`、`traynor01` 的 cargo 覆盖。
- `Maps/XM/Launcher.SC2Map` 的候选链路仍需按 source 重新打包验证。

## 剩余事项

1. 重新打包并验证 Launcher 候选列表。
2. 进游戏确认标准基地图与特殊地图的初始兵种 / 建筑替换。
3. 抽测 Zagara 英雄 4 个主动技能和对应按钮状态。
4. 回归确认新增 helper 不会让无基地图重复给兵。

## 最小可跑路径

1. 先确认 launcher source 和 packed 版本一致。
2. 再跑标准基地图。
3. 然后跑无基地 / 运输 / 特殊分支图。

## 后续检查点

- `CommanderAch/Zagara` 是否真的落到真实升级 ID。
- `PlayerCommanders/ZergZagara` 是否在 runtime 可读、可用。
- 标准地图是否能稳定生成 `ZagaraVoidCoop` 和基础虫群开局。
- 顶部面板是否能稳定显示核心技能按钮。
- 特殊地图的空投 / 运输 / 英雄支援分支是否走到 Zagara helper，而不是落回默认人族或其他已移植指挥官。
- 特殊地图分支是否不会回落到默认种族 helper。

## 风险提醒

- 不要把 `MutatorAmonZagara` 这类 AI / 突变因子对象当成可玩 commander 来源。
- 现阶段很多 Zagara 名字只代表“对象存在”，不代表“链路已通”。
