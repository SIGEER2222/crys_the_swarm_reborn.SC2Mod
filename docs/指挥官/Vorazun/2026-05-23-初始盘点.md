# Vorazun 2026-05-23 初始盘点

日期：2026-05-23

## 进度记录

- `XMAlarak.SC2Mod` 作为 Vorazun 数据 owner，已补齐核心 mastery / dummy / requirement / validator 链。
- `XMFinal` 已接入 `Commander == "Vorazun"` 的 runtime 分支、基地初始化和地图给兵 helper。
- `XMCore` 已接入 `CommanderAch/Vorazun` 和本地化字符串。
- `tools/launcher_mpq` 与 `LauncherAuto.SC2Map` 已同步加入 Vorazun 候选。
- `Maps/XM` 的 Vorazun 特殊地图分支已补入。

## 已接对象

### XMFinal

- `SoACasterVorazun` 初始化
- `ConsoleProtoss_Nerazim` 控制台皮肤
- `VorazunCreateMapStartSquad`
- `VorazunCreateCargoSquad`

### XMCore

- `CommanderAch/Vorazun` 8 个升级
- `CommanderAch/Vorazun` 6 个精通

### XMAlarak

- `MasteryVorazunStartingAndMaxSoAEnergy`
- `MasteryVorazunChronoBoostSpeed`
- `MasteryVorazunShadowGuardDuration`
- `MasteryVorazunBlackHoleDuration`
- `MasteryVorazunDarkPylonRadius`
- `MasteryVorazunTimeStopHaste`

## 剩余事项

1. 跑 `scripts/validate-vorazun-port.ps1`
2. 实机确认标准基地图和特殊地图是否都能正常进场
3. 需要时再补 live 入口同步

## 风险提醒

- 不要把 `crys_the_swarm_reborn.SC2Mod` 当来源，只能当写法参考。
- `XMFinal` 的 `DocumentHeader` / `DocumentInfo` 不要再碰。
