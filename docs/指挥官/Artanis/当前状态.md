# Artanis 当前状态

日期：2026-05-23

这份文档记录 Artanis 当前已经落地的移植状态。当前版本已经从“缺核心链路”推进到“独立模组、CommanderAch、XMFinal runtime、Launcher 候选和特殊地图 helper 都已接上”的阶段。

## 已经成立

### 独立模组与依赖

- `合作指挥官版起义狂潮/Mods/XM/XMArtanis.SC2Mod` 已创建。
- `scripts/import-official-artanis-data.ps1` 已从官方 `starcoop.sc2mod` 导入 Artanis 相关 catalog 对象。
- 导入摘要：`references/official-artanis-import-summary.tsv`
- `XMFinal.SC2Mod/DocumentInfo` 与 `DocumentHeader` 已加入 `file:Mods\XM\XMArtanis.SC2Mod`。

### Launcher 候选

- `tools/launcher_mpq` 已包含 Artanis 候选。
- 当前候选顺序已经推进到 Artanis：
  - `gv_commanderNum = 12`
  - `CommanderPreset` 三个 `Count` 都为 `13`
  - `Commander Index="12"` = `Artanis`
  - `Por Index="12"` = `UserData/CommanderPreset/ID_Por_012`
  - `CommanderPortrait Index="12"` = `Assets\Textures\ui_btn_commanderportrait_artanis.dds`

### 成长与运行时

- `XMCore.SC2Mod/Base.SC2Data/GameData/UserData.xml` 已有 `CommanderAch/Artanis`。
- 8 个成就升级映射到 Artanis 官方升级对象：
  - `ArtanisCommander`
  - `SOAOrbitalStrike`
  - `SOAHeroicShield`
  - `SOAWarpTech`
  - `SOAWarpGateCharges`
  - `ArtanisUnlockHighArchon`
  - `ArtanisUnlockReaver`
  - `ArtanisUnlockTempest`
- 6 个巅峰项映射到 Artanis mastery 对象：
  - `MasteryArtanisShieldOvercharge`
  - `MasteryArtanisShieldOverchargeCDR`
  - `MasteryArtanisSoAPowerFieldHaste`
  - `MasteryArtanisWarpChargeCooldown`
  - `MasteryArtanisGuardianShellHeal`
  - `MasteryArtanisStartingAndMaxSoAEnergy`
- `XMFinal` 已识别 `Commander == "Artanis"`，并从 `PlayerCommanders/ProtossArtanis` 应用默认能力、默认升级和皮肤。
- `InitializeBase` 已创建 `SoACasterArtanis`、调用 `CU_GPInit("Artanis")`、显示顶部面板，并生成 Artanis 轻型开局部队链。
- `XMCore` 已把 `CU_GPInit("Artanis")` 映射到通用 Protoss 顶栏初始化，避免 Artanis caster 创建后落入空分支。
- `XMFinal` 已新增 Artanis 地图 helper：
  - `libE0EAE146_gf_ArtanisCreateMapStartSquad`
  - `libE0EAE146_gf_ArtanisCreateMapStartSquadInRegion`
  - `libE0EAE146_gf_ArtanisCreateCargoSquad`

### 地图分支

以下特殊地图已接到 Artanis helper：

- `thanson01`: `ArtanisCreateCargoSquad("hero")`
- `thanson02`: `ArtanisCreateMapStartSquad("hero")`
- `thorner02`: `ArtanisCreateCargoSquad("hero")`
- `thorner03`: `ArtanisCreateMapStartSquad("hero")`
- `thorner05s`: `ArtanisCreateMapStartSquad("heavy")`
- `traynor01`: `ArtanisCreateMapStartSquad("light")`、`ArtanisCreateCargoSquad("light")`
- `ttosh03b`: `ArtanisCreateMapStartSquad("heavy")`
- `ttychus02`: `ArtanisCreateCargoSquad("heavy")`、`ArtanisCreateCargoSquad("hero")`
- `ttychus03`: `ArtanisCreateMapStartSquad("air")`
- `ttychus04`: `ArtanisCreateMapStartSquad("air")`
- `ttychus05`: `ArtanisCreateMapStartSquad("air")`
- `tvalerian01`: 按原点位补 `ArtanisCreateMapStartSquad("heavy"/"light")`

### 单位、建筑、英雄、面板

已静态命中的关键对象包括：

- 基地/工人：`Nexus`、`Probe`
- 主要建筑：`Pylon`、`Gateway`、`CyberneticsCore`、`RoboticsFacility`、`Stargate`
- 核心部队：`ZealotAiur`、`Dragoon`、`HighTemplar`、`ImmortalAiur`、`Reaver`、`PhoenixAiur`、`Tempest`、`CarrierAiur`
- 顶部 caster：`SoACasterArtanis`
- 英雄单位：`SMX2ProtossArtanis`
- 面板能力：`SOAPylonPower`、`SOAOrbitalStrike`、`SOASuperShield`、`SOAStrafeAttack`
- 训练链已补当前阶段必要覆盖：
  - `GatewayTrain`: `ZealotAiur`、`Dragoon`、`HighTemplar`
  - `RoboticsFacilityTrain`: `ImmortalAiur`、`Reaver`
  - `StargateTrain`: `PhoenixAiur`、`Tempest`、`CarrierAiur`

### Guardian Shell

- `SOAHeroicShieldInvulnerableApply` 已补到实际行为链。
- `SOAHeroicShieldImmunity` 现在带 `5` 秒无敌窗口，并在结束后进入 `SOAHeroicShieldWeakness`。
- `SOAHeroicShieldWeaknessApply` 已补出，避免只挂了名字没有实际冷却/弱点链。
- `SMX2ProtossArtanis` 已接入 `AllUnitBehaviorController` 和 `Guardian Shell` 相关按钮/图标，作为可出场英雄单位使用。

## 已验证

已通过：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-artanis-port.ps1 -RequireXMFinalDependency -RequireLauncherCandidate
```

验证结果：

- `XMArtanis.SC2Mod` 的关键 Unit/Abil/Button/Upgrade/Commander/UserData 对象。
- `SMX2ProtossArtanis` 的英雄单位定义、图标和状态条。
- `XMCore CommanderAch/Artanis` 的基地、农民、成长映射和中文文案。
- `SOAHeroicShield` / `SOAHeroicShieldImmunity` / `SOAHeroicShieldWeaknessApply` 的 Guardian Shell 链。
- `XMFinal` 的 Artanis runtime、helper 声明与实现。
- `XMFinal` 的 `DocumentInfo` / `DocumentHeader` 的 `XMArtanis` 依赖。
- `tools/launcher_mpq` 与 packed `Launcher.SC2Map` 的 Artanis 候选、数量、头像引用。
- 特殊地图 Artanis helper 分支。
- live 目录已同步 `XMCore.SC2Mod`、`XMFinal.SC2Mod`、`XMArtanis.SC2Mod`、`XMFenix.SC2Mod`、相关特殊地图和 `Launcher.SC2Map`。
- live packed `Launcher.SC2Map` 解包后确认 `gv_commanderNum = 12`、`CommanderPreset Count=13`、`Commander Index=12 = Artanis`、`ID_Por_012` 均存在。

## 剩余风险

- 尚未做本轮实机点击验证。
- Artanis 的完整官方 `libCOMI` 细节仍比当前 XM runtime 更深，当前版本是官方数据对象 + XMFinal 最小 runtime，已能支撑面板、训练链、地图 helper 和成长链。

## 下一步建议

1. 进一次实机 Launcher，选择 Artanis。
2. 进一张标准基地图，确认 `Nexus/Probe/Pylon` 和顶栏按钮。
3. 再抽一张无基地图和一张运输图，确认不回落默认单位。
