# 凯瑞甘当前状态

日期：2026-05-22

这份文档基于原始 `kerrigan-port-progress-2026-05-22.md` 收敛而成，只保留当前仍值得继续使用的结论。

## 已经成立

### 模组与数据导入

- `XMKerrigan.SC2Mod` 已创建。
- 仓内已有官方 Kerrigan 相关数据导入结果。
- 原始记录中提到的关键对象包括：
  - `K5Kerrigan`
  - `K5KerriganBurrowed`
  - `CoopCasterKerrigan`
  - `PrimalSlash`
  - `MindBolt`
  - `PsiStrike`
  - `PsionicLift`
  - `KerriganAssimilationAura`

### 依赖链

- `XMFinal.SC2Mod` 当前已依赖 `XMKerrigan.SC2Mod`。
- 这点很关键，因为旧记录里最容易误导人的部分就是“凯瑞甘入口已经加了，但可能没进 `XMFinal` 依赖链”；按当前仓内扫描，这个问题已经修正。

### 公共接线

- `XMCore` 已存在 `CommanderAch/Kerrigan`。
- `CU_GPInitKerrigan` 已接入公共面板分发。
- Kerrigan 的升级和 mastery 映射已经写入 `XMCore` 用户数据。

### 战役运行时

- `XMFinal` 已存在 `Kerrigan` 的初始化分支。
- 当前代码已经具备：
  - `CoopCasterKerrigan` 初始化入口
  - 英雄或开局相关分支
  - 基础 runtime 接线

### 地图分支

原始进度文档提到的几张关键地图分支已经有对应实现记录，包括：

- `traynor01`
- `ttosh03b`
- `tvalerian01`
- `thanson01`
- `thorner02`
- `thorner03`
- `thorner05s`
- `ttychus02`
- `ttychus03`
- `ttychus04`
- `ttychus05`

## 当前仍需收口

### 面板仍是通用虫族模板

按当前 `CU_GPInitKerrigan` 实现：

- 仍使用 `ZergCasterPanelTemplate`
- 内层仍是 `ZergGlobalCommandPanelTemplate`
- 只初始化 2 个按钮

所以它现在更像“runtime 已有入口的第一阶段实现”，还不是完整的凯瑞甘专属顶部面板。

### Live 验证还不能省

当前仓内结论主要能证明：

- 依赖链在
- 数据对象在
- 入口函数在
- 地图分支在

但还需要继续确认：

- `K5Kerrigan` 是否稳定创建
- `CoopCasterKerrigan` 是否真的显示正确按钮
- 英雄技能链是否能正常施放
- 标准基地图和特殊剧情图是否都没有落回默认逻辑

### 旧文档中的“缺依赖”判断已经过时

原始旧稿中最重要但已经过时的一条是：

- “`XMFinal.SC2Mod/DocumentInfo` 缺少 `XMKerrigan.SC2Mod`”

当前仓内已不再符合这条判断，因此不能再继续沿用旧稿直接下结论。

## 当前最准确的判断

凯瑞甘已经不是纯规划状态，而是：

- 模组已建
- 依赖已接
- `CommanderAch` 已接
- 地图分支已开始铺开

但它也还不应被表述成“已完整可交付的正式指挥官”，因为英雄、面板和实机玩法链还需要进一步验证。

## 建议后续顺序

1. 先用标准基地图确认开局、英雄和 `CoopCasterKerrigan`。
2. 再测一个基础主动技能。
3. 再回头看 `traynor01`、`ttosh03b`、`ttychus03` 这类特殊分支图。
4. 最后才补专属面板样式和更细的玩法收口。

## 历史资料

原始阶段稿已移动到 `archive/kerrigan-port-progress-2026-05-22-原始稿.md`。

它仍保留导入范围、逐图记录和阶段判断，但不再适合作为当前入口文档。
