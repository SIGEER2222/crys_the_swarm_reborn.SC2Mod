# 凯瑞甘当前状态

日期：2026-05-23

## 2026-05-28 补充口径

- 当前请以 `docs/每日进度/2026-05-28-Abathur-Alarak-Kerrigan-Zeratul完成确认与兵种建筑面板.md` 为最新交接口径。
- `Kerrigan` 当前严格静态校验已再次通过；现阶段应视为“静态链路已闭环，待统一 live 复验”。
- 后续重点不再是“凯瑞甘是否接进来了”，而是以 `K5Kerrigan` 为唯一真值做统一实机回归。

## 已完成

### 独立模组与数据
- `XMKerrigan.SC2Mod` 已创建并配置完整。
- 包含完整的 K5 技能数据：
  - **PrimalSlash（原始之刃）**: PrimalSlashJumpOnly、PrimalSlashPointSearch、PrimalSlashStun 等效果
  - **MindBolt（精神螺栓）**: MindBoltDamage 效果和相关行为
  - **PsiStrike（灵能冲击）**: 相关效果和升级
  - **PsionicLift（灵能之掀）**: 相关效果和升级
- 包含 K5 升级：
  - `K5PrimalSlash` - 原始之刃强化
  - 威望相关升级（CommanderPrestigeKerrigan*）
- 包含 K5 单位：
  - `K5Kerrigan` - 凯瑞甘英雄单位
  - `K5KerriganBurrowed` - 潜地凯瑞甘
- 包含 K5 按钮：
  - `PrimalSlash` 按钮
  - `MindBolt` 按钮

### 运行时与初始化
- `XMFinal` 已包含凯瑞甘的初始化分支
- `CoopCasterKerrigan` 已从 live 必需项移除（避免重复的空壳面板）
- K5Kerrigan 的命令卡通过单位定义中的 `LayoutButtons` 配置

### 面板决策
- **已决定**: 不显示通用的 `CoopCasterKerrigan` 顶部面板
- **原因**: K5Kerrigan 英雄本体已有完整的命令卡（PrimalSlash、M indBolt 等），再显示空壳顶栏会造成混乱
- **当前方案**: 依赖 K5Kerrigan 本身的英雄命令卡

### 特殊地图分支
已确认以下地图有凯瑞甘分支记录：
- `traynor01`
- `ttosh03b`
- `tvalerian01`
- `thanson01`
- `thorner02`
- `thorner03`
- `thorner05s`
- `ttychus02`
- `ttychus03`
- `ttychus04`
- `ttychus05`

### 依赖链
- ✅ `XMFinal.SC2Mod/DocumentInfo` 已包含 `XMKerrigan.SC2Mod` 依赖
- ✅ `XMCore` 包含 `CommanderAch/Kerrigan`
- ✅ `CU_GPInitKerrigan` 已接入面板系统（但实际不使用）

## 待完善

### 特殊地图逻辑需实机验证 ⚠️

虽然地图分支有记录，但需要确认：
- 这些特殊地图的凯瑞甘开局是否真正使用凯瑞甘逻辑
- 而不是落回默认的人族或占位逻辑

### K5 英雄技能稳定性 ⚠️

需要确认：
- K5Kerrigan 是否稳定创建
- 英雄命令卡是否正常显示
- PrimalSlash、M indBolt 等技能是否可正常施放

### 威望数据验证 ⚠️

需要验证以下 ID 是否正确：
- `VoidCoopHeroicFortitude` (非 `KerriganHeroicFortitude`)
- `CommanderPrestigeKerriganCreep` (非 `CommanderPrestigeKerriganMalignantCreep`)
- `MasteryKerriganAutoAttackDamage` (非 `MasteryKerriganAttackDamage`)

## 当前状态评估

可以确认的是：
- ✅ K5 英雄技能数据完整
- ✅ 面板决策已明确（不显示空壳顶栏）
- ✅ 依赖链和初始化已接入
- ✅ 特殊地图分支有记录
- ⚠️ **特殊地图逻辑需要实机验证**
- ⚠️ **威望 ID 需要验证**

## 建议后续顺序

1. **高优先级**: 用标准基地图确认 K5Kerrigan 英雄和技能
2. **中优先级**: 测试一个特殊剧情图（如 thanson03b）
3. **中优先级**: 验证威望 ID 和效果
4. **低优先级**: 逐图确认特殊地图分支

## 建议后续顺序

根据状态文档的建议：
1. 用标准基地图确认开局、英雄和 `CoopCasterKerrigan`（已移除空壳）
2. 再测一个基础主动技能
3. 再回头看 `traynor01`、`ttosh03b`、`ttychus03` 这类特殊分支图
4. 最后才补专属面板样式和更细的玩法收口

## 历史资料

原始阶段稿已移动到 `archive/kerrigan-port-progress-2026-05-22-原始稿.md`。

它仍保留导入范围、逐图记录和阶段判断，但不再适合作为当前入口文档。
