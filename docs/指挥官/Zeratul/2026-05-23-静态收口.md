# Zeratul 2026-05-23 静态收口

## 当前完成

- 官方合作 Zeratul 数据已从 `references\official-casc-export\mods\starcoop\starcoop.sc2mod` 合并进入 `XMRaynor.SC2Mod`。
- `XMFinal` 已补齐 Zeratul 运行时入口：
  - `ApplyZeratulCommanderRuntime`
  - `Initialize` 指挥官识别
  - `InitializeBase` 顶部面板与英雄创建
  - `ZeratulCreateMapStartSquad`
  - `ZeratulCreateCargoSquad`
- `XMCore` 已补 `CommanderAch/Zeratul` 和 8+6 成长文案。
- Launcher 源和 `LauncherAuto` 已对齐为 15 个候选，新增 Zeratul 候选和头像索引。

## 本轮补齐

- 补了 `XMRaynor.SC2Mod` 的最小中文串：
  - `UserData/PlayerCommanders/ProtossZeratul_*`
  - `CoopCasterZeratul`
  - `ZeratulACArtifact`
  - `ZeratulCoop`
  - `ZeratulNexus`
  - `ZeratulProbe`
  - `ZeratulMapWideStasis`
- 补了以下地图的 Zeratul helper 分支，避免落入默认分支或旧占位：
  - `ttychus03`
  - `ttosh03b`
  - `tvalerian01`
  - `ttychus04`
  - `ttychus05`
  - `thorner05s`
  - `thorner03`
  - `thanson02`
  - `thorner02`
  - `ttychus02`
  - `thanson01`

## 当前结构判断

- 数据承载仍在 `XMRaynor.SC2Mod`，这是临时现实，不是最终理想归宿。
- 运行时所有者已经是 `XMFinal`，后续继续补技能/事件时应优先放在 `XMFinal`。
- Launcher 之前存在计数漂移：
  - UserData 候选数和 `gv_commanderNum` 不一致
  - 本轮已统一修正

## 剩余风险

- 尚未做本轮 live 验证，仍需确认：
  - 选择界面能否稳定显示 Zeratul
  - 顶部面板按钮是否可用
  - 基地、农民、建筑是否完全替换为 Zeratul 体系
  - 神器储放台、传奇军团、静滞射线是否按预期工作
- `XMRaynor` 仅补了最小中文串，若 live 中继续出现英文或对象 ID，需要继续补 localized keys。
- 目前只补了已有 Protoss helper 风格地图；若还有其他硬编码地图未覆盖，需要继续通过 `rg` 搜 commander 分支。

## 建议的下一步

1. 先运行 `scripts\validate-zeratul-port.ps1`
2. 静态通过后再做一次集中 live 验证
3. live 验证重点先看：
   - launcher 选人
   - 标准基地图
   - `ttychus03` 空投图
   - `thorner02` / `ttychus02` cargo 图
